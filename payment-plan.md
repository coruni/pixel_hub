# 支付与收费点 · 实施方案（公益向，待审核）

> 定位：**公益向资源社区**。收费的目标是「覆盖成本 + 给创作者发钱」，不是从用户身上榨取利润。
> 配套文档：`creator-incentive-plan.md`（资金出口：激励池 → PIX → 用户自助提现）。
> 状态：**方案待审**，未动任何代码。

---

## 0. 结论先行

### 三条设计原则（先立原则，再定收费点）

1. **成本分摊，不是价值捕获**——只在"产生了真实成本"或"资源要流向创作者"的地方收费。参考线：这项收费能否对应到一张账单（服务器/存储/带宽）或一笔要打给作者的钱？不能，就不收。
2. **不卖地位**——付费只买「鸣谢标识」和「容量」，**永远不换贡献分、等级、榜位、免审、加急审核**。等级只能靠贡献挣。这条一旦破，整个激励体系的公信力就没了。
3. **够用即止**——达到当月成本目标后，前台自动收起募捐入口（可配）。公益站的正确姿态是"本月够了，不用再捐"。

### 收费点：只做 2 个

| # | 收费点 | 资金去向 | 一期 |
|---|---|---|---|
| 1 | **站点赞助**（自愿，任意金额） | 覆盖成本 → 余额进激励池 | ✅ |
| 2 | **容量扩容包**（大文件/图床空间） | 抵真实存储带宽成本 | 二期（需先有配额体系） |

**打赏不算收费点** —— 打赏只能用站内 **PIX** 转账（见 `creator-incentive-plan.md` §2.5），不经过支付通道、平台不经手真钱。

### 明确不做（红线）

| ❌ 不做 | 原因 |
|---|---|
| 内容付费墙（资源/文章付费可见） | 违背公益定位，内容必须免费 |
| 下载限速、下载次数限制 | 拿"能不能正常用"换钱，是最典型的资本家做法 |
| 付费去广告 | 自相矛盾——广告收入正是发给创作者的池子来源 |
| 花钱买贡献分 / 等级 / 榜位 / 免审 | 破坏激励体系公信力（原则 2） |
| 付费加急审核 | 插队，破坏公平 |
| 会员专享评论/私信等基础社交 | 基础功能不切分 |
| 强制付费才能注册/看评论 | 同上 |

### 支付通道

对接**易支付**（彩虹/好收米系标准网关），**只用于收入进站**，不用于出款（提现打款走线下，见 `creator-incentive-plan.md` §2.4）。落 4 个服务端接口 + 2 张表。全部技术细节见 §3。

---

## 1. 为什么是这 2 个（成本对账）

站点的真实成本项与对应收费点：

| 真实成本 | 谁在消耗 | 对应收费点 |
|---|---|---|
| 服务器（Vercel）+ 数据库（Supabase）+ 域名 | 全站访客 | 站点赞助 |
| 图片存储（Chevereto/S3/本地盘） | 上传图片的作者 | 扩容包（二期） |
| 大文件存储与下载带宽（OneDrive/云盘） | 上传大附件的作者 | 扩容包（二期） |
| —（不产生平台成本） | 创作者的内容被别人看 | 无 —— 打赏是站内 PIX 转账，平台不经手真钱，不构成收费点 |

没有出现在这张表里的东西，就不该有收费点。

---

## 2. 收费点详设

### 2.1 站点赞助

| 项 | 设计 |
|---|---|
| 入口 | `/fund` 资金池公示页的赞助区块（`#sponsor`）+ 页脚「支持本站」+ 侧栏 widget（可配）；旧 `/sponsor` 301 到此 |
| 金额 | 预设 ¥5 / ¥10 / ¥20 / ¥50 + 自定义（下限 ¥1，上限可配） |
| 留言 | 可附一句留言（≤60 字，需过滤敏感词，展示前过审） |
| 匿名 | 支持匿名（鸣谢墙显示为「一位热心网友」） |
| 权益 | ① 鸣谢墙署名与「赞助者」标识（纯展示）② 无任何功能特权 |
| 资格 | 无需登录也可赞助；登录赞助才计入鸣谢墙与标识 |
| 公告 | 在 `/fund` 首屏公示「本月成本 / 已筹 / 缺口」（同一页可核对到每笔明细），达成后收起入口（原则 3） |

**关键**：赞助是**捐赠**性质，不是消费。文案、按钮、页面标题全部按捐赠写，不写"购买""升级""解锁"。

### 2.2 打赏已移出收费点

打赏从「读者花钱」改成「读者用自己挣的 PIX 打赏」，因此**不再是收费点**，也不再经过支付通道：

- 计价与扣款：站内 PIX，从打赏者的可用余额直接扣 —— **平台不经手任何真钱**
- 平台收益：0（原本也不抽成，现在连资金都不经手）
- 完整规则见 `creator-incentive-plan.md` §2.5（含防互刷设计：转账净额为零、不产生贡献分）

这样处理有三个好处：① 读者不必为"支持作者"而掏钱，打赏回归纯粹的认可表达；② 平台少一层代收代付资金流，合规面收窄；③ 打赏不新增 PIX，**不影响偿付水位**。

### 2.3 容量扩容包（二期）

前置工程：当前 `UploadLimits`（`src/lib/upload-limits.ts`）是**全站统一**的单文件大小上限，**没有任何 per-user 配额**。要做"扩容"必须先有"免费额度"。

因此二期先建配额体系（用户已用存储统计 + 月度上传流量），再做扩容包：

| 项 | 建议 |
|---|---|
| 免费额度 | 单文件上限沿用现有配置；总容量给一个宽松的免费额度（如 2GB） |
| 扩容 | 按容量档位购买（如 +10GB / ¥5 / 30 天），**不作废已上传内容**，到期仅限制新上传 |
| 边界 | 超额后**不删除、不隐藏**已有内容（红线：不用用户的存量内容当人质） |

一期不做，避免为了收钱先给用户套上枷锁。

---

## 3. 易支付对接（协议已核实）

### 3.1 网关与配置

网关地址、商户 ID、商户密钥三项**全部走后台配置**，按 `runtime-config.ts` 既有模式扩展（后台配置 > env 回退，密钥不进前台）：

| 配置项 | 说明 |
|---|---|
| `epayEnabled` | 总开关，关闭时前端不显示任何付费入口 |
| `epayUrl` | 网关地址（如 `https://xxx.com`，请求时拼 `/mapi.php`、`/submit.php`） |
| `epayPid` | 商户 ID |
| `epayKey` | 商户密钥（**仅服务端读取，绝不进前台 bundle、绝不进日志**） |

同时更新 `sanitizeRuntimeConfig()`（URL 去尾斜杠）与 `runtimeConfigIssues()`（地址合法性校验），落点在 `src/lib/runtime-config.ts` 与 `/admin/runtime` 页面。

### 3.2 接口清单

| 用途 | 我们的路由 | 上游接口 |
|---|---|---|
| 创建订单下单 | `POST /api/pay/create` | `POST {网关}/mapi.php`（返回 JSON，取 `payurl` / `qrcode`） |
| 异步回调（发货） | `GET /api/pay/notify` | 易支付主动 GET 回调，**必须返回纯文本 `success`** |
| 同步跳转落地页 | `GET /pay/result` | 用户支付后浏览器跳回 |
| 主动查单（兜底） | `GET /api/pay/status` | `{网关}/api.php?act=order&pid=&key=&out_trade_no=` |
| 退款（后台） | `POST /api/pay/refund` | `{网关}/api.php?act=refund`（需后台权限 + `AuditLog`） |

### 3.3 签名算法（上游规定的原文）

1. 取**发送或接收到的全部参数**（不含 `sign`、`sign_type`，**不含空值**），按参数名 ASCII 升序排序；
2. 拼成 `a=b&c=d&e=f`，**参数值不做 URL 编码**；
3. 末尾直接拼接商户密钥，`sign = md5(拼接串 + KEY)`，取 **32 位小写**。

下单与验签用的是**同一套**算法（下单签名 → 发出去；回调验签 → 收回来），实现必须复用同一个函数，不写两遍。

### 3.4 回调参数

| 参数 | 说明 |
|---|---|
| `pid` | 商户 ID —— **必须校验与本商户一致** |
| `trade_no` | 易支付订单号 |
| `out_trade_no` | 我们的商户订单号（查单依据） |
| `type` | `alipay` / `wxpay` |
| `name` | 商品名称 |
| `money` | 金额（**元**，字符串，2 位小数） |
| `trade_status` | **仅 `TRADE_SUCCESS` 视为成功** |
| `param` | 透传参数（我们用来带订单号校验，见下） |
| `sign` / `sign_type` | 签名 |

### 3.5 八个必须做到的工程要点

1. **验签是前提**：回调先验签，再谈发货。用 `node:crypto` 的 `timingSafeEqual` 做恒定时间比较，不用 `===`（防时序侧信道）。
2. **金额必须比对**：回调 `money` 解析成「分」后，必须等于订单表里的 `amountFen`，不等直接拒（防金额篡改）。**用字符串解析转分，不用 `parseFloat(x) * 100`**（浮点误差）。
3. **`trade_status` 严格判定**：只有 `TRADE_SUCCESS` 发货，其余原样返回。
4. **幂等**：易支付会重复推送。订单状态机 `PENDING → PAID` 单向，已 `PAID` 的订单直接返回 `success`，不二次发货、不二次计账。
5. **必回 `success`**：纯文本小写 `success`（不是 JSON、不是 200 空响应），否则上游持续重试。
6. **订单号不可预测**：`out_trade_no` 用 `cuid()`/随机串，**绝不用自增 ID 或用户 ID 拼接**（否则可枚举、可伪造回调）。
7. **主动查单兜底**：回调可能丢失。`/pay/result` 页加「我已支付，刷新状态」按钮，点它服务端调 `api.php` 查单补偿。不依赖常驻 cron。
8. **密钥不出服务端**：`api.php` 查单按上游设计需在 URL query 里带明文 `key`，因此 ① 只允许服务端调用 ② **不把完整请求 URL 写进日志** ③ 错误上报时脱敏。

另外：**`notify_url` 必须公网可访问**。本地开发无法直接联调，需公网隧道或在 Vercel 预览环境验证 —— 这条写进验收说明。

---

## 4. 数据模型（Prisma 增量）

```prisma
/// 支付订单（唯一事实来源；微信/支付宝侧只负责把状态推回来）
model PaymentOrder {
  id          String        @id @default(cuid())
  outTradeNo  String        @unique  // 发给易支付的商户订单号（cuid，不可预测）
  kind        OrderKind
  userId      String?                // 付款人；匿名赞助可为空
  user        User?         @relation(fields: [userId], references: [id], onDelete: SetNull)
  amountFen   Int                    // 金额，单位：分
  subject     String                 // 提交给易支付的商品名
  periodKey   String?                // 归属期 YYYY-MM（赞助计入激励池的收入归属）
  status      OrderStatus   @default(PENDING)
  // 上游回执
  epayTradeNo String?
  payType     String?                // alipay / wxpay
  notifyRaw   String?                // 回调原始 query（审计；不含 key）
  paidAt      DateTime?
  refundedFen Int           @default(0)
  // 展示与风控
  message     String?                // 赞助留言
  anonymous   Boolean       @default(false)
  ipHash      String?                // 加盐哈希（不存明文 IP），供刷单排查
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  @@index([status, createdAt])
  @@index([kind, periodKey])
  @@index([userId])
}

enum OrderKind {
  SPONSOR      // 站点赞助（进成本覆盖 + 激励池）
  QUOTA        // 容量扩容（二期）
}

enum OrderStatus {
  PENDING
  PAID
  CLOSED       // 超时未支付关闭
  REFUNDED
}

/// 收支台账：成本与收入逐笔记录，支撑「本月成本 / 已筹 / 缺口」公示与对账
model LedgerEntry {
  id        String   @id @default(cuid())
  periodKey String   // YYYY-MM
  kind      String   // COST_SERVER / COST_STORAGE / COST_DOMAIN / INCOME_SPONSOR / INCOME_QUOTA …
  direction String   // IN（收入） / OUT（支出）
  amountFen Int
  note      String?
  createdBy String?
  createdAt DateTime @default(now())

  @@index([periodKey, direction])
}
```

**与激励方案的接口**：`INCOME_SPONSOR` 台账 + `PaymentOrder(kind=SPONSOR, status=PAID)` 共同构成激励池的收入来源，`periodKey` 决定进哪一期池子。打赏不走这里（站内 PIX 转账），因此支付订单**只有「收入进站」一个方向**；出款全部由激励方案的提现口负责。收入进账需同步写 `LedgerEntry(direction=IN)`，供偿付水位（`creator-incentive-plan.md` §3.4）计算。

迁移沿用仓库约定：手工 SQL `prisma/migrations/0007_payment.sql`，`CREATE TABLE IF NOT EXISTS` + 枚举 `DO $$ ... $$`，不改存量数据。

---

## 5. 前台

**赞助入口已并入 `/fund` 资金池公示页**（详设见 `creator-incentive-plan.md` §8.1）—— 「钱去哪了」与「我要给钱」必须同屏，拆成两页只会多一次跳转、多一点怀疑。

| 页面/组件 | 内容 |
|---|---|
| **`/fund`**（锚点 `#sponsor`） | 赞助区块：为什么需要钱（成本明细表）、本月进度条、金额选择、留言、匿名开关、FAQ（钱去哪了 / 我不捐会影响使用吗）。同页另有收支流水、各期结算公示、鸣谢墙（`#thanks`） |
| `/sponsor` | **301 → `/fund#sponsor`** —— 页脚、二维码、历史外链不失效 |
| `/sponsors` | **301 → `/fund#thanks`** |
| 页脚 | 「支持本站」入口指向 `/fund` + 当前筹资进度一行文案 |
| 资源详情 `ActionBar` | 「打赏」按钮 —— 站内 PIX 转账，UI 与实现见 `creator-incentive-plan.md` §8，本文档不涉及资金 |
| `/pay/result` | 支付结果落地页：成功/处理中/失败三态 + 「我已支付，刷新状态」兜底按钮 |

**文案纪律**（公益站的调性）：一律用"赞助""支持""请作者喝杯汽水"，禁止出现"VIP""开通会员""升级""解锁""特权""尊享"。所有付费入口的文案必须能通过这句自检：**把"付费"两个字换成"捐赠"，这句话还成立吗？**

进度条形态遵守现有视觉语言：`rounded-none` 直角、`brand-500` 填充、无渐变。金额用 `¥` + 两位小数。

---

## 6. 后台

### `/admin/payment`（adminOnly）

| 区块 | 内容 |
|---|---|
| 通道配置 | 易支付开关 / 网关 / 商户 ID / 商户密钥（密钥输入框默认掩码）+ 连通性自检（调 `act=query` 验签） |
| 收费点开关 | 赞助开关与金额档位、上下限（打赏的开关与范围在 `/admin/incentive`） |
| 订单管理 | 列表（状态/类型/金额/时间/付款人）、详情（含回调原文）、手动补单（需备注 + `AuditLog`）、退款 |
| 成本台账 | 逐月录入成本项（服务器/存储/域名），驱动前台筹资进度 |
| 对账 | 我方订单合计 vs 上游 `act=orders` 批量查单结果，差异高亮 |

### 现有页面改动

- `/admin/runtime`：不改结构（易支付配置独立成 `/admin/payment`，避免把 runtime 页撑爆）。
- `/admin/layout.tsx`：新增「支付」tab + `AdminTabs.tsx` 图标映射。

---

## 7. 模块划分

| 新模块 | 职责 |
|---|---|
| `src/lib/epay.ts` | 易支付协议层：`buildSign()` / `verifySign()` / `createOrder()` / `queryOrder()` / `refund()`。**纯服务端**，密钥只在这里被读取 |
| `src/lib/payment.ts` | 订单领域层：创建订单、状态流转、发货（幂等）、台账读写 |
| `src/lib/actions/payment.ts` | 后台 server actions（配置、补单、退款、录成本） |
| `src/app/api/pay/*` | 四个路由（create / notify / status / refund） |

`epay.ts` 与 `payment.ts` 分开的理由：协议层只认签名与 HTTP，领域层只认订单与状态机 —— 换支付通道时只动协议层。

---

## 8. 分期

### 支付一期（本次要做）

1. `runtime-config.ts` 扩展易支付配置 + `/admin/payment` 通道配置与自检
2. 数据模型 + `0007_payment.sql` 迁移
3. `epay.ts` 协议层（签名复用同一个函数）+ `payment.ts` 订单层
4. 四个 API 路由（下单 / 回调 / 查单 / 退款）
5. **`/fund` 页面落地**：赞助区块 + 鸣谢墙 + 收支明细（`#sponsor` / `#thanks`）+ `/sponsor`·`/sponsors` 301 + 页脚入口 + `/pay/result` 落地页
6. `/admin/payment` 订单管理 + 成本台账
7. 筹资进度与水位对外公示（`/fund` 首屏；水位算法在激励方案 `coin.ts`，此处只读）
8. 订单 `PAID` 后自动写 `LedgerEntry(direction=IN)`，供偿付水位（激励方案 §3.4）计算

### 支付二期（扩容包，需前置配额体系）

9. per-user 存储配额与用量统计
10. 扩容包下单与到期处理（到期只限新上传，**不作废存量内容**）
11. 权益在超限场景的降级体验（明确提示而非静默失败）

---

## 9. 验收

| 项 | 方式 |
|---|---|
| 类型/静态 | `tsc --noEmit` 零错误；eslint 不新增 warning |
| 构建 | `npm run build` |
| 签名一致性 | 临时脚本 `prisma/_epay-test.ts`（用完即删）反证：① 下单签名串与手工按规范拼的结果逐字符相同 ② 回调验签对篡改金额/篡改订单号/错误密钥三种情况全部拒绝 ③ 空值参数不参与签名 |
| 金额精度 | 反证 `"0.1"`、`"1.1"`、`"1.15"`、`"999.99"` 元解析成分为 10 / 110 / 115 / 99999，无浮点误差 |
| 幂等 | 同一回调连推 5 次：订单只发货一次、**收入台账只记一次**、`success` 每次都返回 |
| 金额篡改 | 构造金额不一致的回调：拒绝发货且记异常日志 |
| **付费不换地位** | 反证断言：支付成功后该用户 `UserPoint.balance` 与 `level` **完全不变**（守住 §0 原则 2） |
| 密钥不泄露 | 全站搜索确认 `epayKey` 不出现在任何 client component、日志与页面输出中 |
| 路由兼容 | `/sponsor`、`/sponsors` 均 301 到 `/fund` 对应锚点，页脚与历史外链不 404 |
| 迁移幂等 | `npx prisma db execute` 连执行两次无报错 |
| 回调联调 | 需公网可达地址；本地用隧道，或在 Vercel 预览环境验证回调与返回 `success` |
| 视觉 | 真机量测 320 / 375 / 390 / 430 / 768 / 1024 / 1440px + 明暗双主题 |

---

## 10. 待你拍板（3 项）

1. **易支付的具体服务商**：网关地址、商户 ID、密钥要给我才能联调（密钥只写进后台配置或 env，不进代码与提交）。
2. **赞助是否登录可见鸣谢**：未登录也能赞助，但只有登录赞助才上鸣谢墙与获得标识 —— 同意吗？
3. **成本公示范围**：公示到"总成本一个数"，还是明细到"服务器/存储/域名各多少"？后者更透明但等于公开你的账单结构。

---

## 11. 风险与取舍

| 风险 | 处置 |
|---|---|
| **易支付的合规风险** | 易支付系产品多为第四方聚合支付，**没有央行支付牌照**，存在服务商跑路、资金冻结、被卷入他人非法结算的风险。建议：① 不要把大额资金留在易支付账户，**到账即提现** ② 主通道优先考虑有牌照的正规渠道（个人可用合规捐赠/赞助平台，或具备资质后接支付宝当面付等），把易支付作为**备用通道** ③ 保持"捐赠/赞助"定位而非"销售"，页面文案与发票处理都按捐赠写 |
| 经营性备案 | 若被认定为经营性收款，可能涉及 ICP 经营性备案与纳税义务。整体按公益捐赠定位可降低风险，但**合规结论请自行确认或咨询专业人士，系统不提供合规担保** |
| 回调丢失 | 主动查单兜底按钮 + 后台对账（我方订单 vs 上游批量查单）双保险 |
| 刷单/自充 | 赞助也计入激励池收入，理论上可自充刷池再分回。缓解：`ipHash` 留存、后台异常提示（同一 IP 高频小额）、金额上限配置 |
| 密钥泄露 | 密钥只存后台配置/env，只被 `epay.ts` 读取；查单 URL 不打日志；代码搜索列为验收项 |
| 打赏纠纷 | 打赏为站内 PIX 转账且不可撤回（激励方案 §2.5），不涉及退款、平台不经手真钱，无代收代付纠纷 |
| 提现的税务与主体 | 系统不代扣代缴；对累计提现金额较大的创作者，建议先要求提供收款与身份信息再发放（详见激励方案 §14） |
| 付费污染社区氛围 | 鸣谢墙保持克制；打赏榜默认关闭；禁止任何"按打赏额排序"的榜单设计 |
