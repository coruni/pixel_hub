# Pixel Hub 后台数据管理基础功能补齐

## 全局约束

- 只修改后台数据管理相关源码与测试；不修改公开 API、数据库 schema、迁移或依赖。
- 沿用 App Router、Prisma、Server Actions、现有 `staff`/`adminOnly`、`audit`、`useAction` 和直角视觉组件。
- 删除仅使用软删除；所有写操作必须校验权限、输入并刷新相关路径。
- 现有主工作区改动不属于本任务；提交时只包含本 worktree 的功能改动。

## Task 1：内容库检索、分页与编辑

- 修改内容库页面，增加关键词、类型、作者、分类、状态筛选，默认每页 30 条，稳定按 `createdAt desc, id desc` 分页并显示总数。
- 新增 `/admin/content/[id]/edit` 页面和可复用编辑表单。
- 新增 `updateResourceAdminAction`，允许版主和管理员编辑标题、简介、正文、分类、标签、下载地址、类型元数据和可见性开关。
- 使用现有 Zod meta schema 校验；事务同步分类/标签关联与标签计数；slug、作者、媒体、统计及历史版本保持不变。
- 复用现有上下架 action，增加内容库到编辑页的入口。
- 为新增 action 和分页/标签同步行为添加可执行测试，先验证失败再实现。

## Task 2：用户管理检索、筛选与分页

- 修改用户管理页面，增加用户名/昵称/邮箱关键词搜索、角色筛选、封禁/正常筛选、免审筛选。
- 默认每页 30 条，稳定按 `createdAt desc, id desc` 分页并显示总数，保留 ADMIN-only 与当前角色/免审/封禁动作。
- 不新增资料编辑、账号删除或公开 API。
- 添加查询解析、分页边界和权限场景测试，先验证失败再实现。

## Task 3：媒体库筛选、分页与孤儿批量清理

- 修改媒体库页面，增加文件名、上传者、媒体类型、处理状态、孤儿媒体筛选；保留 30 条分页并稳定保留所有筛选参数。
- 增加孤儿媒体多选 UI 和批量清理 action；仅 ADMIN 可调用。
- 服务端逐条确认媒体未关联资源、评论或头像后删除，数据库记录与存储文件清理沿用现有逻辑，失败需返回结果并写审计。
- 引用媒体、头像引用、非法 ID 和空选择均不得被删除；添加 action 和查询测试，先验证失败再实现。

## 最终验证

- 运行 `npx tsc --noEmit`、`npm run lint`、`npm run build`、`npm run test:smoke`。
- 检查 `git diff --check`、工作区边界、权限守卫、审计、响应式表格和分页链接。
