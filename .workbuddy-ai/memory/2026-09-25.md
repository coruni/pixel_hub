# 2026-09-25

- 为 Pixel Hub 接入同端口 WebSocket：自定义 Node 服务器与进程内总线；站内通知、资源评论信号、在线状态实时广播；浏览器端单例连接、退避重连与 HTTP 降级；Docker/README 已同步。
- 调整个人主页头部操作布局：编辑/打赏改为昵称后的纯图标，关注按钮独立固定在资料区右侧，昵称标题取消截断以允许自然换行。
- 发布向导与改稿统一将 GAME 纳入单封面类型：游戏/文章/音乐/视频最多一张封面，IMAGE 保留预览图组；切换类型和恢复旧草稿时自动收敛多余游戏预览图，服务端校验、草稿说明和编辑页同步。
- 生成并接入 Pixel Hub 风格默认封面 `public/covers/default-cover.webp`（暖白、赤陶橙、像素点阵、游戏/文章/图片/工具图标），无封面卡片、列表、详情和音视频 poster 统一使用。
- 完成侧边栏系统审查：确认 home/archive/detail 三套配置与内容 slots 共用渲染管线；发现分类/标签/创作者/统计与首页模块重复，详情 sameCategory 与正文 RelatedSection 重复，并记录创作者排序保存、tier 不可编辑、游客空栏等配置问题。
- 修复游客空侧栏：父页面先解析实际侧栏 ReactNode 再交给 SidebarLayout，登录专属模块被游客过滤后不再创建空 grid 列；sidebarVisible 同步纳入 authed 判断。
- 新增文章详情「文章目录」侧栏模块：从正文 h2-h4 标题提取目录，标题不足 3 个自动隐藏；Markdown 标题统一生成 article-heading-N 锚点；默认详情侧栏启用，后台可配置开关/标题/设备可见性。
- 优化首页「主推精选」：第一个资源继续固定大图主推，后续副推新增后台可调的「小卡片网格/紧凑列表」样式，以及小/中两档尺寸；旧配置自动使用小卡片小尺寸。
- 优化文章目录：有子标题的章节支持原生 details 折叠，h3/h4 通过嵌套列表自动缩进，目录链接仍保持正文锚点跳转。

## 收尾排查（截至 11:42，master 与 origin/master 同步、工作区干净）
- 真 bug：侧栏「人气创作者」widget 保存时只回传 `{count}`（WidgetEditor.tsx:118），漏存 `creatorSort`/`period`，后台改了排序/周期保存后丢失。【已修复】WidgetEditor.tsx:118 改为 `return { count, sort: creatorSort, period };`；`site-config.ts` 的 creators 类型(277)与 creatorsCfg schema(~189-194) 本就声明并校验这两个字段，schema 层会保留，整链路已通；`tsc --noEmit` 通过。
- 支付/fund 已落地实现，但 `payment-plan.md §10` 有 3 项待用户拍板（易支付网关/商户ID/密钥、赞助登录可见鸣谢、成本公示范围），外加回调联调需公网地址；未拍板无法真上线。
- OneDrive 集成：`DriveManager` 标注 Graph 三件凭据未配置，登记盘仅占位，附件仍走 STORAGE_DRIVER。
- 安全审计：`next.config.ts` 已落地 H1 安全响应头（报告 §6 已过时）；H2+M2 限流/验证码集中存储、M1 广告 HTML 净化、M3 角色守卫强制化、L1 bcrypt 升 12、L4 依赖审计仍未做。
- 验证缺口：`package.json` 的 `test:smoke`/`test:admin-data` 指向 `_test/` 目录，但该目录不存在，脚本会失败；AGENTS.md 验收要求跑 smoke。
- 其它 agent 分支未合主干：`feat/pinch-zoom-hero-avatar`、`trae/agent-H5XBfr`、`codex/admin-data-management`、`codex/ai-site-steward`。
