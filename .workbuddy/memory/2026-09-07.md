# 2026-09-07

## 首页「为你推荐」推荐算法重构

重写 `src/lib/queries.ts` 的 `getRecommendations`，从「标签/分类相似度」升级为带用户画像的精准推荐 + 抗信息茧房。

**核心改动（5 文件，+601/-56）：**
- `src/lib/queries.ts`：新增 `buildRecProfile`（综合 点赞1.0/收藏2.0/评论1.5/关注1.2 加权 + 90天半衰期时间衰减，覆盖 tag/cat/type/author 四维 + engagedIds 去重）、`scoreCandidates`（相似度+质量+新颖度打分）、`itemSim` + `diverseSelect`（探索槽位 + MMR 多样性选择 + 最少分类覆盖兜底）。候选池合并质量池(250) + 新鲜池近180天(250) 去重。
- `src/lib/home-config.ts`：recommend 配置新增 `explorationRatio`(0-0.6默认0.3) 与 `minCategories`(0=自动) 字段，向后兼容。
- `src/components/home/blocks/recommend.tsx` + `HomeRenderer.tsx`：透传新配置到 `getRecommendations`。
- `src/components/home-admin/SectionEditor.tsx` + `HomeManager.tsx`：后台新增「探索占比」「最少分类覆盖」控件与摘要。

**设计要点：**
- 弱画像/冷启动自动提升探索占比（×1.4，封顶0.7），强画像收敛到精准。
- 游客/scope=all/无信号 → 回退全站热门，保证不空白。
- 验证：`npx tsc --noEmit` 与 `npm run lint` 在我改动文件内零错误（仅存量 `prisma/import-galgame.ts` 报错，非本次范围）。

**注意：** 尚未 commit/push（用户未发「提交推送」指令）。

## GAME 资源详情横幅样式微调
- 需求（用户原话）：游戏类型详情页 `absolute inset-x-0 bottom-0 p-5 sm:p-6` 遮罩改为和其他遮罩一样样式；简介最多两行；删除横幅里重复的 download 按钮，保留描述上方 ActionBar 的下载，避免下载按钮过多。
- 改动 `src/components/resource/detail/DetailBanner.tsx`：
  1. 遮罩渐变：原为 `rgba(0,0,0,.88) 0-55%` + 中段发灰 `.4` 55-75% + transparent，改为与卡片遮罩一致的干净两段式 `linear-gradient(to_top,rgba(0,0,0,.92)_0%,rgba(0,0,0,.92)_55%,transparent_100%)`，并加 `pointer-events-none`。
  2. 简介（detail.summary）：加 `line-clamp-2` 限最多两行。
  3. 删除横幅内的 `<DownloadButton externalUrl>` 块及其 import（重复下载按钮）；描述上方 `ActionBar`（parts.tsx:127）的 GAME 下载保持不变。
- 附带修复 `prisma/import-galgame.ts`（上一轮迁移脚本）的 tsc/lint 报错使其不影响全仓校验：顶部加 `eslint-disable no-explicit-any`；`import pg` 加 `// @ts-expect-error`（pg 无类型，仅 tsx 运行）；`statusMap` 类型改为 `Record<string,ResourceStatus>`（导入 ResourceStatus）；`tags.map` 参数标注 `Record<string,any>`。（注：`@ts-nocheck` 被 ban-ts-comment 规则禁止，故改逐项修复。）
- 验证：`npx tsc --noEmit` ✅0 错误；`npm run lint` ✅0 error（8 warning 均存量，非本次文件）；dev server(3000) 渲染已发布 GAME 资源 `/resources/cracktrick` 返回 200，HTML 含新渐变 `linear-gradient(to_top,rgba(0,0,0,.92)` 与 `line-clamp-2`，无 500。截图因浏览器工具未安装（需下载 ~500MB Chromium）未做，以服务端 HTML 校验为准。
- 状态：全部未提交（分支 master）。

## 修复编译错误（JSX 解析 + tsc 全树）
- 用户反馈 `./src/components/admin/UploadLimitsManager.tsx (361:19)` 报 `Expected '</', got 'jsx text'`。
- 根因：第三块「附件格式限制」(line 292) 以 `<div className="border-b ...">` 直接开在 grid 内，但 line 361 的 `</section>` 无对应开标签——缺一层 `<section>` 包裹（其他两块 line 199/246 均为 `<section>` 包 header+body）。修复：在第三块前插入 `<section className="overflow-hidden border border-brand-200 bg-surface">`，让 line 361 的 `</section>` 正确闭合。
- 顺带修掉同轮 tsc 暴露的另外两个真错误（都不是本次引入，属仓库存量）：
  1. `src/components/upload/UploadWizard.tsx:72` 重复声明 `const maxCount`（line 58 已声明，line 72 冗余重声明导致 TS2448/2454），删除该行。
  2. `src/lib/actions/social.ts:330` 引用未定义常量 `COMMENT_IMG_MAX_COUNT`，改为已算好的 `commentMaxCount`（上限截断）。
- 验证：`npx tsc --noEmit` ✅0 错误；`npm run lint` ✅0 error（8 warning 均存量，非本次文件）。
- 状态：全部未提交（分支 master）。
