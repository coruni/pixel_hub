# 2026-09-11

## 图片查看器：缩小时无法拖动（已修复并提交）
- 根因：`src/components/ui/ImageViewer.tsx` 平移判定用 `zoomRef.current > 1` 当「内容溢出」的代理条件，缩小时鼠标拖动直接落入 `ignored`；且缩放/旋转变化都不重新夹取位移，放大拖动后再缩回会停在偏移位置且拖不动。
- 修复：新增 `panBounds / clampOffset / canPan`，以图片原始布局框（`offsetWidth/Height`，不受 transform 影响）为基准算可平移空间；鼠标/笔只要有空间即可拖动，触摸在非放大态保留滑动切图；`applyZoom`/`rotateBy` 后重新夹取位移；拖动位移超过 3px 才抑制 click，保留「点黑色背景关闭」。
- 坑：react-hooks v7 的 immutability 规则禁止在 useCallback/useEffect 里写 `useRef.current`（useLayoutEffect 内允许），旋转改为直接读 `rotation` state，删掉了 rotRef。
- 状态：tsc + eslint 通过；浏览器交互复验由用户在本地自测。

## 上传压缩：webp/jpg/png 可选 + 质量 + alpha 保留（已实现）
- 新增 `src/lib/media/compress.ts` 作为唯一压缩出口：webp 锁 `alphaQuality: 100`；png `quality<100` 走 palette 量化（显式带 alpha 支持）、`=100` 无损；jpg 无 alpha，先 `flatten` 白底再 mozjpeg 编码。
- 配置扩展 `SiteSetting["uploadLimits"]`：新增 `imageFormat`(webp/jpg/png，默认 webp) 与 `imageQuality`(1..100，默认 82)；后台 `/admin/uploads` 新增「图片压缩」区块（格式单选 + 质量输入 + alpha/体积说明）。
- 四条链路接入：图集大图与缩略图、评论附图（`media.mime` 改为按格式动态）、头像、主页横幅；缩略图质量自动 −8（默认 74，与原值一致）；落盘 key 扩展名随格式变化。
- 新增回归脚本 `_test/compress-alpha.ts`（`npx tsx`）：实测 webp/png 输出 `hasAlpha=true`、半透明 128 与全透明 0 均保持，jpg 透明区合成 rgb(255,255,255) 白底。
- 未做：完整上传 E2E（会写入真实存储后端，留待用户自测）。

## OneDrive 附件下载名丢失（已修复）
- 根因：附件落盘路径由 `cloudRelKey(ext)` 生成 `YYYYMM/{uuid}.{ext}`，原名只在 `Media.fileName` 里；而下载 `/api/dl` 对 OneDrive 直接 302 到 Graph 预鉴权链接，`Content-Disposition` 由 SharePoint 取**云盘存储名**生成 → 用户保存到本地的是 uuid 名，`n` 参数对 302 分支无效。
- 技术约束（已查证）：302 直连下无法指定下载名 —— `download.aspx` 只有 `download=1`（强制下载 vs 打开）这类布尔参数，`SourceUrl=` 只认真实文件路径。要让下载名 100% 保真就必须让字节经过服务端，而 Vercel 函数时长/带宽扛不住 200MB~250GiB 附件。结论：**存储名必须保留原名**。
- 定案（用户拍板）：落盘名 = `[rootPath/]YYYYMM/{安全化原名}_{uuid}{ext}`。原名在前（云盘可读、下载即原名），uuid 后缀保证全局唯一 → 无需探测重名、无并发覆盖；截断只作用于原名主体，uuid 与扩展名完整保留。
- `src/lib/storage/onedrive.ts`：新增 `sanitizeCloudName`（非法字符 `" * : < > ? / \ |` 与控制字符、首尾点空格、超长截断；保留名 CON/PRN/AUX/NUL/COM0-9/LPT0-9、`~$`、`_vti_`、`.lock` 均加下划线规避）与 `cloudItemPathFor`（同步纯函数）；取代此前的异步探测版 `resolveCloudItemPath`。
- 去重：session 与 legacy 两条上传路由新增查询 —— 同 `uploaderId + kind=ATTACHMENT + fileName + size + READY` 命中则直接返回已完成记录（`{ok:true, deduped:true, url, name, size}`），跳过整轮 Graph 上传；浏览器直传场景服务端不接触字节，无法算内容 hash，故以「同名同大小」为同一文件判据。客户端 `upload-attachment-client.ts` 识别 `deduped` 分支。
- `src/app/od/[driveId]/[...key]/route.ts`：原 `SEG` 只允许 `[A-Za-z0-9._-]`，会把中文/空格/括号原名直接 400；放宽为「非空、≤120 字符、非 . / ..、不含路径分隔与控制字符」。
- 验证：`_test/cloud-name.ts` 20+ 用例（中英文/非法字符/保留名/超长/点分日期/tar.gz/200 次同名唯一性）全通过；真实请求 od 网关对中文、空格、括号、`#`、`%`、原名_uuid 六类路径均返回 404（通过校验并查盘），修复前为 400；tsc + 全部改动文件 eslint 干净。
- 遗留：完整上传+下载 E2E 需真实 OneDrive 凭据，由用户自测。

## 侧边栏 / 首页模块「防杂乱」设计（已实现）
- 分析结论：侧栏与主栏是「同一信息空间的两个分层」——主栏负责「看什么」（大图+信息流），侧栏负责「去哪/社区脉动」（定位+排行+数据）；杂乱感主要源于**同一维度信息在两处重复**（如分类、标签、资源列表）。
- 去重：首页 `home-config.ts` 的 `categories` 板块默认 `enabled:false`，分类入口统一由侧栏 `分类直达`（sticky 常驻）承担。
- 侧栏分层折叠：给 `SidebarWidget` 加 `tier: "primary" | "more"`（默认 primary，写入 `parseWidget`）；`SiteSidebar.renderWidgets` 将 `more` 收进原生 `<details>`「更多模块 ▾」折叠组（无 JS、可键盘、加 `motion-reduce:transition-none` 降级）。默认 `tags` 设为 `more` → 首页侧栏仅常驻 热门内容 + 分类直达，标签云折叠。
- 验证：`tsc --noEmit` + eslint（三改动文件）+ `next build` 全过，仅 `api/dl/route.ts` 既有动态文件访问 warning（非本次改动）。

## 推荐「探索/随机」模式 + 模块可见性（已实现）
- 推荐模块加 `mode: "personalized" | "explore"`（默认 personalized）：explore = 不依赖画像，对候选池（质量池+新鲜池，排除已互动）按「质量+新颖度」取前 3×count，Fisher-Yates 洗牌后抽 count，每次刷新不同；`queries.ts` 内 `noStore()` 保证逐请求实时计算（不被静态/路由缓存冻结）。重构出 `buildCandidatePool`/`exploreSelect`/`shuffle`/`popularFallback` 辅助函数，personalized 路径复用 `buildCandidatePool`。
- 模块可见性字段「端可见 + 是否登录可见」同时加到**首页板块**与**侧栏 widget**两套系统：
  - 首页：`HomeSection`(Prisma) 加 `visibleOn TEXT default 'all'` + `requireAuth BOOLEAN default false`（迁移 SQL `prisma/migrations/0002_home_section_visibility.sql`）；`home.ts` View / `action/home.ts` `HomePatch` / `SectionEditor` / `admin/site/page.tsx` viewRows 全链路贯通。
  - 侧栏：`SidebarWidget` 加 `visibleOn?`/`requireAuth?`（`site-config.ts` `parseWidget` 解析）；`SiteSidebar`/`WidgetArea` 接收 `authed` 过滤 requireAuth 并按 `visibleOn` 包裹响应式容器；`action/site.ts` `SidebarWidgetPatch` + `WidgetEditor` 控制项贯通；三处调用方（`app/page`、`resources/[slug]`、`feed/ArchiveShell`）传入 `authed`。
  - 设备端可见用 Tailwind 响应式类实现（服务端安全、无需 UA 嗅探）：`visibleOnClass` = pc→`hidden lg:block`、mobile→`block lg:hidden`、all→无；`requireAuth` 走服务端过滤（未登录直接不渲染，无信息泄露）。
- 注意：DB 列迁移需手动执行 `prisma/migrations/0002_*.sql`（或 `prisma db push`）；`prisma generate` 因查询引擎 dll 被占用报 EPERM，但 TS 类型已刷新、build 仍通过——运行时客户端沿用既有引擎即可。验证：tsc + eslint(15 文件) + next build（40/40 页）全过。

## 热门排序加「时间窗口」period（已实现）
- 背景：确认「热门」= `getFeed({sort:"popular"})` 按 `likeCount` 累计降序、无任何时间窗（全时间）。用户要求给**所有用到 popular 排序的模块**都加近期热门窗口。
- 新增共享类型 `HotPeriod="all"|"week"|"month"` + 标签/工具（`periodDays`/`toFeedPeriod`）于 `site-config.ts`。
- 贯通清单（全部默认 `all`=累计全时间）：
  - 侧栏 `hot` widget：配置加 `period`，`renderHot` 透传给 `getFeed`。
  - 侧栏详情页 `authorWorks`/`sameCategory`：配置加 `period`，`renderAuthorWorks`/`renderSameCategory` 透传。
  - 首页 `recommend` 板块：配置加 `period`，`RecommendBlock`→`getRecommendations`；`buildCandidatePool` 用 `publishedAt>=窗口` 收紧候选池（取与 180 天新鲜池更紧者，避免覆盖），`popularFallback` 透传 `getFeed`；探索/个性化/回退三种路径都生效。
  - 编辑器：`WidgetEditor`（hot + authorWorks/sameCategory）、`SectionEditor`（recommend）均加「热度时间窗口」下拉。
- 默认 `DEFAULT_SIDEBAR_WIDGETS`/`DEFAULT_DETAIL_WIDGETS` 字面量补 `period:"all"` 以过类型检查。
- 未动 `getRelated`（详情页相关推荐，按相关度打分、非独立热门模块）。
- 验证：tsc + eslint(8 文件) + next build（40/40 页）全过，仅既有 `api/dl/route.ts` warning。
