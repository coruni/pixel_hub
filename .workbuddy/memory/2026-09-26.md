# 2026-09-26 工作日志


## 评论区接入 Markdown 编辑器（2026-09-26）

**需求**：评论输入框改用站内现有 Milkdown Crepe 编辑器（MdEditor），允许 Markdown 基础语法，
但编辑器内禁止上传图片。

**已确认决策**（用户四选）：
1. 只关 ImageBlock（表格/链接/代码块保留）
2. 手打 `![x](url)` 外链图**接受**，风险留到渲染层白名单兜底
3. 评论附图入口（服务端压缩/水印链路）**保留**
4. 工具栏**整个去掉**，保留 `/` 斜杠菜单

**改动文件**：
- `src/components/rte/MdEditor.tsx`：新增 `features` / `toolbar` / `compact` 三个 prop，
  features 从硬编码改为 `{...BASE_FEATURES, Toolbar: toolbar, ...features}` 合并；
  ImageBlock 关闭时不再注册 onUpload（条件展开 featureConfigs）。
  注意 `Crepe.Feature` 作**类型**不可用（TS2702），类型注解要用 `CrepeFeature` 枚举，运行时用 `Crepe.Feature`。
- `src/components/social/Comments.tsx`：主楼 textarea → MdEditor（minHeight 6rem、
  toolbar=false、compact、features 关 ImageBlock+Table）；加 COMMENT_MAX=2000 字数计数
  （仅剩 200 字时出现，与 social.ts commentSchema 同口径按源码字符数）；
  Ctrl/Cmd+Enter 提交；发表成功靠 `editorKey` 自增重建编辑器清空内容（defaultValue 仅挂载消费）。
  **修复 blob 泄漏**：原 pick/remove 无脑重建 previews 从不 revokeObjectURL，
  改为 syncPreviews 先 revoke 旧再建新 + 卸载时统一释放（previewsRef 需在 useEffect 里同步，render 中赋值会被 react-hooks/refs 拦）。
- `src/components/social/comment-item.tsx`：评论正文从 `whitespace-pre-wrap` 纯文本改为
  `<Markdown zoomable>` 渲染，外层 `md-body-wrap`；附图从 `max-h-40` 不定宽改为
  24×24 等高切片 + `i/n` 序号角标；回复框仍是单行 input（`commentInputCls` 保留）。
- `src/app/globals.css`：新增 `.md-editor[data-compact]` 段（14px/内距 10px 12px）与
  `.md-body-wrap .md-body` 评论级排版收敛段（标题压 0.95rem、图片限高 16rem）。

**验证**：tsc --noEmit 通过、eslint（social + rte）通过、next build 通过。

**遗留**：渲染层图片域名白名单（决策 2）**尚未实现**，当前外链图画什么就渲染什么。

**追加（13:02）**：用户反馈编辑器左侧露出块操作柄（+ / ⠿），要求删掉。
已在 COMMENT_FEATURES 增加 `[CrepeFeature.BlockEdit]: false`。
注意：Crepe 的 BlockEdit 特性**同时**提供块操作柄（block handle）与 / 斜杠菜单（menu），
二者由同一特性注册（见 feature/block-edit/index.js 末尾 .use(menuAPI).use(block).use(menu)），
关掉即两者一起消失——评论框不依赖 / 组块，已同步删掉占位文案里的「输入 / 唤出块类型」提示。

**追加（13:26）**：落实对照图里的「拖拽上传」。评论输入区接上站内现有 hook。
- `Comments.tsx` 引入 `useFileDrop` / `useFilePaste`（`src/lib/hooks/`，全站上传入口复用同一对），
  挂在整个输入区容器（`relative mt-4`）上，而不是只挂附图按钮——拖到编辑器上、缩略图上、
  按钮上任意位置都算数，与 media-picker.tsx 的做法一致。
- 拖拽态：整块盖一层 `border-2 border-dashed border-brand-500 bg-brand-50/95` +
  「松开即可添加附图」。**必须 pointer-events-none**，否则遮罩自己会触发新的 dragenter/dragleave
  把深度计数器搅乱。像素站点不用毛玻璃。
- disabled 条件：`sending || imagesFull`（imagesFull = imageMax<=0 或已达上限），
  与 input 的 disabled 语义对齐——满额时拖进来的图会被 slice 静默丢弃，不如不接管。
- 附图入口文案补「拖入或粘贴也可添加」；拖拽时按钮边框同步转 brand-500。
- 顺带给 `pickImages` 加了 `list.length === 0` 早退（useFilePaste 在剪贴板无图时本来就不调用，
  但 useFileDrop 的 onDrop 有可能拿到空 FileList）。

**追加（13:38）**：用户纠正附图区布局。原实现把「附图 0/4」按钮 + 飘在旁边的
「拖入或粘贴也可添加」文案 + 缩略图全塞在一行 flex 里，视觉杂乱。
改为（对齐 media-picker.tsx 的虚线格形态）：
- **无图**：只渲染一个「附图 0/4」入口按钮，其余不出现。
- **有图**：上面仍是入口按钮（显示计数），下面另起一行渲染缩略图 80×80 +
  一个 80×80 虚线「＋ 添加 / 拖入或粘贴」方框，提示文案**收进方框内**，不再飘在外面。
- 添加方框与入口按钮**共用同一个 input**：给 input 加 `id="comment-image-input"`，
  方框用 `htmlFor` 关联（一个 input 不能在两处渲染，只能这样复用）。
- 缩略图从 56→80（h-20 w-20），与方框同尺寸对齐。
- `imagesFull` 时方框不渲染，与 useFileDrop/useFilePaste 的 disabled 条件保持同步，
  避免「框还在但拖进去没反应」。拖拽态文案在方框内变「松开即可」。

**追加（14:20）**：梳理评论区加载/子评论/超长列表逻辑并出图（未改代码）。
结论存档，供后续做评论分页时参考：
- **评论区没有分页**。`src/lib/queries.ts` 的 `getResource` 内 `prisma.comment.findMany`
  一次 `take: 200`（`orderBy createdAt desc` 后 `.reverse()` 转正序），展平后经
  `src/components/resource/detail/parts.tsx` 把 `detail.comments` 全量交给 `Comments`。
  无 offset / cursor / hasMore，超出 200 条的部分不加载也不提示。
- 展平口径：`rootIdOf` 沿 parentId 上溯到根；**父楼层被删导致上溯链断裂时，该条自成根楼层**
  （若只按 `!parentId` 判根，这类回复会被静默吞掉——库里仍是 PUBLIC 但页面看不到）。
  深层回复 `replyTo` 只指向父楼层（二级回复为 null）。
- 更新路径：WS `comment:new` → 增量 `GET /api/comments?since=`（只取新增）；
  WS `comment:changed` → `router.refresh()`；删除兜底靠返回的 `liveIds` 与本地 id 差集；
  兜底轮询 60s（WS 在线）/ 15s（降级）。`take: 200` 是**软上限**，分页改造要连带处理
  `liveIds` 差集（它按全量 id 比对，分页后本地只有子集会误判删除）。
- 若要做「加载下一页」：项目已有通用 `src/lib/hooks/use-load-more.ts`（page/done 用 ref +
  busyRef 在飞闩，Feed 与首页 `list-more.tsx` 在用），复用即可，无需新架构。

## 评论区加载更多 + 子评论分页（2026-09-26 14:45）

**需求**：根评论加分页器；单个根的子评论不再全量铺开，展示部分 + 点「下一页」加载。
**已确认决策**（用户二选）：根评论**最新在上**（desc）、用**页码分页器**（非加载更多按钮）。

**新增/改动**：
- `src/lib/comments-paging.ts`（新）：根楼层判定 `rootFloorWhere` = `parentId: null`
  **或** `parent.status != PUBLIC`（后者是「父被删后回复上移」的口径，必须与展平同一口径，
  否则那些回复在库里是 PUBLIC、页面上却看不到）；`fetchRootCommentsPage`（desc + `(createdAt,id)`
  稳定排序 + 多取一条判 hasMore）；`fetchRepliesByRoot` 逐层 BFS 取某根的全部回复，
  **只跟随 PUBLIC 节点**（非公开楼层就地断链，其子回复由 rootFloorWhere 接住，不会重复挂）；
  `fetchRepliesPage` 单根回复页。
- `src/components/social/comment-types.ts`：`CommentShape` 加 `repliesPaging`；
  抽出 `CommentReply` / `PagingMeta` / `CommentsPaging` / `totalPagesOf`；
  **`ROOT_PAGE_SIZE=10`、`REPLIES_PAGE_SIZE=5` 定义在这里**——取数层依赖 prisma，
  客户端不能引用，而分页器要算总页数。
- `src/lib/queries.ts`：`getResourceDetail` 的「一次取 200 条再内存展平」整段替换为
  `fetchRootCommentsPage(resource.id, 1)`，返回 `comments` + `commentsPaging`（comments 字段名保留兼容）。
- `src/lib/actions/social.ts`：新增 `loadRootCommentsAction` / `loadRepliesAction`（zod 校验，
  资源可见性口径与 `getResourceDetail` 一致：登录用户可预览未发布资源）。
- `src/components/social/CommentPager.tsx`（新）：`CommentsPager` + `RepliesPager`，复用
  `<Button variant="ghost">`；只有 `totalPagesOf > 1` 才渲染（单页摆「1/1」是噪音）。
- `src/components/social/use-comment-polling.ts`：**改成无状态 hook**（`{comments, setComments,
  sinceResetKey, page, onReload, onAdded}`）——列表 state 提到 Comments.tsx，避免
  「hook 返回 setter 但 reload 又要用 setter」的循环依赖。新增根评论只在 `page===1` 时 unshift
  到顶部（desc 排序）；新回复只在「该根当前展示的是最后一页」时追加，否则只涨 total；
  删除兜底从 `router.refresh()` 改成 `onReload()` 重拉当前页（不再把用户弹回第 1 页）。
  **注意**：`liveIds` 差集兜底在新架构下仍然成立——liveIds 仍是全量，本地是子集，
  「本地有、服务端没有」这个方向不受分页影响，别误以为要改成交集检查。
- `src/components/social/comment-item.tsx` / `Comments.tsx`：接分页器与翻页回调。

**验证**：`npx tsc --noEmit` 通过；`npm run lint` 0 error（自己引入的 1 条 unused
eslint-disable 已清）。**build 未跑**（用户明确说不用）。

**顺带修掉的老 bug**：评论作者的 hover 卡片一直不显示「加入于」——原 `toCommentAuthor`
返回的字段名是 `createdAt`，而 `HoverCardUser` 读的是 `joinedAt`。已改对。

**注意事项**：根评论排序由「最早在上（取最新 200 条后 reverse）」变成**「最新在上」**，
这是分页的必然结果（否则新评论永远落在最后一页）；发完评论仍走 `router.refresh()`，
props 变化会把本地重置回服务端给的第 1 页，所以新评论会出现在顶部。

**追加（14:48）**：分页器按反馈改小并换成页码条。
- 尺寸从 `px-3 py-1.5 text-xs`（约 32px 带文字）压到 `h-7 min-w-7`（28px 方形），
  左右翻页改成**纯图标**（lucide Chevron 14px），中间铺页码数字。
- 注意 `<Button>` 的 `buttonClass(tone, size, className)` 在**不传 tone** 时
  `base` 为空串、size 完全不参与 —— 所以这里用 `<Button className={自定义}>` 才能
  完全接管尺寸，别指望用 className 覆盖 `BUTTON_SIZES` 的 `px-3`（同属性 utility
  谁生效取决于生成顺序，不可靠）。
- 页码折叠：`totalPages <= 7` 全列；否则首末页 + 当前页 ±1 常驻、跳段插 `…`。
- 左侧文案精简为「共 N 条评论」（页码条已经表达了位置，再说「第 X/Y 页」是重复）。
- 无障碍：图标按钮 `aria-label`、当前页 `aria-current="page"`、容器 `aria-busy`、
  `role="group"` + `aria-label="评论页码"`；禁用态改走 `disabled:border-neutral-100
  disabled:text-neutral-300`（不吃 `BUTTON_BASE` 的 `disabled:opacity-50`）。
- 验证：`npx tsc --noEmit` 与 `npx eslint src/components/social` 均通过。

**追加（14:49）**：预览图去掉了包裹用的方块容器（`.workbuddy` 里那句「不需要方块容器」
针对的是预览 mockup 的外框，不是代码）。顺带把按钮禁用态的边框从 `neutral-100` 提到
`neutral-200`：禁用按钮在 `#faf9f5` 页底上加 `#f5f5f5` 边框几乎看不见轮廓。

**追加（14:50）**：用户澄清是**分页按钮本身**要去掉 padding / 背景 / border，改纯文字。
- `BTN_BASE` 只留 `inline-flex h-6 items-center justify-center text-xs transition
  focus-visible:ring-2 focus-visible:ring-brand-400 disabled:cursor-not-allowed
  disabled:text-neutral-300`；空闲态 `text-neutral-600 hover:text-brand-700`，
  当前页 `font-medium text-brand-700`。分隔改由容器 `gap-2.5`（10px）承担。
- 保留 `h-6`（24px）做最小点击高度、保留 focus-visible ring（AGENTS.md 硬要求，
  纯文字按钮若去掉焦点环就是可访问性红线）。字号仍 12px。




