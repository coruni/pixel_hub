# 2026-09-14

## 后台用户管理补充头像显示
- 文件：`src/app/admin/users/page.tsx`（唯一改动）。
- 根因/现状：用户列表「用户」列此前是手写首字母方块占位（`bg-neutral-200` + `slice(0,1)`），未走全站统一头像组件，有 `avatarKey` 的用户在后台看不到真实头像。
- 修复：删除手写占位 span，改用 `@/components/ui/Avatar`（`size="sm"`）；查询本来就是 `findMany` 全字段，`avatarKey` 无需额外 select。有图走方角图 + brand 描边，无图回退品牌色首字母方块，与其他页面（Navbar/UserMenu/评论/主页）视觉一致。
- 验证：`tsc --noEmit` 退出码 0（无输出）；ESLint 退出码 0，6 条 warning 全为存量（seed-bulk/seed-extra/admin-media/SiteSidebar），本文件 0 问题。
- 注意：本机 PowerShell/`npx` 子进程 stdout 无法回显，校验时需 `... | Out-File <file>` 再读文件，且 `npx` 输出会被吞 → 直接用 `node node_modules\typescript\bin\tsc`、`node node_modules\eslint\bin\eslint.js .` 调用本地二进制才拿得到结果与退出码。
