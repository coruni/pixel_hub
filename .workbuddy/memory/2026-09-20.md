# 2026-09-20

## 修复音视频（MUSIC / VIDEO）上传「无法正常使用」（对齐 ARTICLE / GAME）

用户报：「上传视频还是无法正常使用 请参考文章 或者 游戏类型 修复上传」（上一轮 9508ae1 只修了 accept 与进度条）。

- **根因 1（决定性）—— 服务端字段错误名与前端读取的 key 对不上，报错被静默吞掉**
  `avMetaSchema`（`src/lib/meta.ts`）校验失败时 `path: ["url"]`，即 `fieldErrors.url`；
  而 `av-section.tsx` 读的是 `fieldError(fieldErrors?.avUrl)` → 永远 undefined。
  结果：视频没传完就点提交 / 提交了空地址 → 服务端返回 `{fieldErrors:{url:[...]}}` → **页面毫无反馈**，看起来「点了没反应」。
  对照：GAME/ARTICLE 读的分别是 `downloads`、`mediaIds`，与 schema 一致，所以它们不犯这个错。

- **根因 2 —— AvSection 没有向宿主上报「上传在飞」，提交按钮不会锁**
  `AttachmentListEditor` 通过 `onBusyChange(inflight)` 让宿主禁用提交 + `onSubmit` 里 preventDefault 兜住回车；
  AvSection 没接这个契约（`UploadWizard` / `ResourceEditForm` 的 `attachBusy` 一直是 0）。
  于是「大视频还在传就点发布」必然走到根因 1 的静默失败。

- **根因 3 —— 上传成功回执被元数据抓取阻塞**
  `onFile` 原来 `setMsg(已上传…)` 排在 `await probeFile()` 之后；大视频读 loadedmetadata 最坏等 12s 超时，
  这期间 `uploading` 仍为 true，按钮一直停在「上传中…」，像卡死。

- **根因 4 —— `AttachmentUpload` 先清空 input.value 再用 FileList**
  `const fl = e.target.files; e.target.value = ""; onFiles(fl)`：Chrome 的 `input.files` 是挂在元素上的**同一份** FileList，
  先清空会把已捕获的引用一起清掉 → 消费方拿到空列表 → 点选文件「没反应」。改为先 `onFiles(fl)` 再清空。

- **改法**

  | 文件 | 改动 |
  |---|---|
  | `src/components/upload/av-section.tsx` | 新增 `onBusyChange?` prop（`uploading ? 1 : 0`）；`fieldErrors?.avUrl` → `?.url`；上传入口从行内按钮改为 `variant="dropzone"` + `useFileDrop`（`disabled: uploading`），与文章/游戏投放区同形；`onFile` 回执不再等 probe，probe 结果异步补进提示 |
  | `src/components/upload/AttachmentUpload.tsx` | `onChange` 里先 `onFiles(fl)` 再 `e.target.value = ""` |
  | `src/components/upload/UploadWizard.tsx` | MUSIC / VIDEO 两处 `AvSection` 接 `onBusyChange={setAttachBusy}` |
  | `src/components/admin/ResourceEditForm.tsx` | 同上（改稿页） |
  | `src/lib/upload-attachment-client.ts` | 旧直传通道失败时带出 HTTP 状态码；413 单独给可读文案（原先只有「文件上传失败」） |

- 验证：`tsc --noEmit` 零错误；eslint（upload / ResourceEditForm / upload-attachment-client）干净。

- **未改（属配置/部署，若仍失败按此排查）**
  1. 上传限流 10 次/小时（`attach:` 与 `attach-session:` 各一条），反复重试会撞 429「上传过于频繁」。
  2. 走旧单请求通道时受宿主请求体上限约束（Vercel 函数约 4.5MB）；大视频要靠 OneDrive 分片直传。
     后台 `/admin/site` 的「附件去向」与「音视频去向」是两个独立开关（`attachmentCloud` / `avCloud`，默认都 auto→跟随存储驱动）——
     只把「附件去向」设成 on 而「音视频去向」留 auto 时，附件走云盘、音视频回退旧通道。

## 续：上传视频「无法使用」的真实根因是**体积上限**，不是上面的契约问题

用户回「按照最合适的修改」。先按无浏览器流程实测复现（起 dev server + 铸管理员 cookie + 真发请求）：

| 请求 | 结果 |
|---|---|
| 946KB `.mp4` → `/attachment` | **200 OK**，落 `public/uploads/files/202609/*.mp4` |
| 300MB `.mp4` → `/attachment` | **413** `当前存储未启用 OneDrive，暂不支持超过 250MB 的文件` |
| 同名同大小重发 | 200 + `deduped:true`（去重生效） |

结论：**音视频服务端链路本来是通的**，小文件能传。卡住的是体积——`uploadLimits.attachmentMaxMb = 2048`（后台 09-11 设的，
界面也写「单文件 ≤2GB」），但 `GRAPH_CLIENT_ID` / `site-runtime.graphClientId` 都已是**空串**，活跃云盘还在、凭据没了，
于是大文件通道静默退化成 `/attachment` 那条「整个请求体读进内存」的单请求通道，硬限 250MB。
视频超 250MB 是常态 → 这就是「无法使用」。同理 `avCloud` 的 auto 分支也没跟附件走（见下）。

- **改动（核心是给「无云盘」补一条流式通道）**

  | 文件 | 改动 |
  |---|---|
  | `src/app/api/upload/attachment/stream/route.ts` **新增** | `PUT`，请求体就是文件字节，`saveStream` 边收边落盘；`?name=&kind=` 走 query；`Content-Length` 缺失 → 411，超 `maxBytes` → 413；不支持流式的驱动 → 501 `NO_STREAM` |
  | `src/lib/storage/types.ts` | `StorageDriver` 加**可选** `putStream(key, body, maxBytes)` |
  | `src/lib/storage/local.ts` | 实现 `putStream`：写 `.uploads-tmp/<uuid>`，成功才 `rename` 到最终路径；流内按累计字节数硬拦 `maxBytes` |
  | `src/lib/storage/index.ts` | 新增 `saveStream()` 与 `streamCapable()`（按驱动有没有 `putStream` 判断，自维护） |
  | `src/lib/av-upload.ts` | `UploadTarget` 加 `stream: boolean` |
  | `src/app/api/upload/attachment/session/route.ts` | 无云盘但 `target.stream` → 200 `{mode:"driver", uploadUrl, maxBytes}`（旧行为是 409 `NO_CLOUD`）；限流**挪到真要建 Graph 会话那一步**（探测不再白扣配额） |
  | `src/lib/upload-attachment-client.ts` | 新增 `putRawBody()`：用 **XHR**（不是 fetch，fetch 读不到上传进度）PUT 整个文件；`uploadAttachment` 三分支：云盘分片 / driver 流式 / 旧单请求 |
  | `src/app/api/upload/attachment/route.ts` | 解析 `formData()` **之前**先看 `Content-Length`：否则 2GB 请求体先把内存吃光再判上限 |
  | `src/lib/runtime-config.ts` | `avCloudEnabled` 的 auto 分支从「看 storageDriver」改成**跟随 `attachmentCloudEnabled`**（附件设 on 而音视频留 auto 时，最需要云盘的大视频反而走了最窄的通道） |
  | `src/components/admin/RuntimeConfigManager.tsx` | 抽出 `attachCloudOn` / `avCloudOn`（与服务端同规则）；音视频说明补「本地存储按附件上限流式直传，s3/Chevereto 仍受约 250MB 单请求限制」 |
  | `.gitignore` | 忽略 `/.uploads-tmp/`（**故意不放 public 下**，避免传到一半的半截视频对外可达；也故意不放 `os.tmpdir()`，跨盘 rename 会 EXDEV） |

- 验证（`prisma/_up3.ts` 探针，13 条断言全绿，跑完已删）：
  300MB 上传 **200**（旧代码同一请求 413，已反证）、落库 size 与磁盘字节数与源一致、成功后临时目录清空、
  超 `maxBytes` 抛错且临时文件清理干净、未超限正常落盘、视频通道仍拒收 `.zip`。
  `tsc --noEmit` 零错误、eslint 干净、`next build` 通过（路由表里出现 `/api/upload/attachment/stream`）。
  探针产生的 Media 记录与落盘文件、`.uploads-tmp/`、`prisma/_*.ts` 已全部清理（`prisma` 下 `_` 文件残留 none）。

- 遗留：s3 / chevereto 没有 `putStream`，仍走 250MB 单请求通道（要支持得各自实现 multipart）。
- 提醒用户：想恢复 2GB+ 且断点续传，把 Graph 凭据填回后台即可走回分片直传；本地/自建则现在直接可用。

