# 2026-09-22

## 创作者激励方案（规划，未实施）

- 产出 `creator-incentive-plan.md`（仓库根，待审核）。
- 现状盘点结论：`User` 无积分/等级字段；`getTopCreators()`（`src/lib/home.ts:139`）只按粉丝数排，首页 `creators` 板块与侧栏 widget 共用；`SiteSetting` 目前只有 `theme` 一个 key；`NotificationType` 复用 `SYSTEM` 即可，无需扩枚举。
- 方案骨架：贡献分（`UserPoint` 物化余额 + `PointLog` 流水，`@@unique([userId, reason, refId])` 做幂等）→ 等级（`levelOf()` 纯函数 + 冗余 `level` 列）→ 徽章 → 榜单（总/月/周）。
- 关键取舍：口径定为「累计获得，取消赞不回冲」；下载仅登录用户每人每资源计一次；**不做**等级自动授予 `User.trusted`（免审属风控）；**不做**消耗式虚拟货币与现金分成。
- 计分挂载点：`approveResourceAction`(moderation.ts)、`toggleLikeAction`/`toggleFavoriteAction`/`addCommentAction`/`toggleFollowAction`/`incrementDownloadAction`(social.ts)。
- 兼容红线：`creatorsCfg` 新字段 `sort` 默认必须落 `"followers"`，否则存量配置行为被动改变。
- 模块划分红线：不得塞进 `src/lib/site-config.ts`（已 762 行，逼近 800 行上限）→ 新建 `src/lib/points-config.ts` + `src/lib/points.ts` + `src/lib/actions/incentive.ts`。

## 创作者激励 v2：加入现金分成（规划，未实施）

- 用户澄清：**要现金激励，但资金来自网站收入按比例提取的池子**，不是自己掏钱。
- 关键事实：站内广告只是 AdSense 类联盟代码注入（`src/components/ads/AdBlock.tsx`），**没有任何收入数据落库**，收入必须管理员手工录入（联盟结算滞后 1–2 月，故归属期与到账日分离）。全站**无支付通道** → 只能「系统出结算单 + 线下打款 + 回填状态」。
- 资金链：收入录入 → `池 = floor((收入+上期结转) × 万分比)` → 按结算分分配 → 快照锁定 → 线下打款 → 公示。
- 硬约束：不透支（收入不录入不产生应发额）、不预支、不自动出款。
- **核心取舍**：荣誉分与结算分共用一份 `PointLog`，靠每个 reason 的「计入结算」开关区分。默认只把**难刷**的计入结算（过审投稿、被收藏、被下载、被精选），点赞/评论/关注默认不计——否则刷分动机从"虚荣"变成"偷钱"。
- 金额精度：一律整数「分」+ 万分比整数；分配用**最大余数法**（保证总额精确等于池子、结果确定性可复算）+ 单人封顶迭代（默认 40%，最多 3 轮收敛）。未发出余款结转下期，账目对平。
- 三道过滤：结算分门槛（默认 ≥50）、最低发放额（默认 ≥5 元）、单人单期封顶（默认 40%）。
- 新增模型：`UserPoint` / `PointLog` / `RevenueEntry` / `IncentivePeriod`（确认即冻结 `configSnapshot` 快照，状态单向 DRAFT→CONFIRMED→PAID）/ `IncentivePayout`。
- 新模块：`money.ts`、`points-config.ts`、`points.ts`、`settle.ts`、`actions/incentive.ts`、`actions/settlement.ts`。后台拆两页：`/admin/incentive`（规则）、`/admin/settlement`（收入+结算台）。
- 已提示税务合规：系统不代扣代缴，由站长自行处理。

## 激励方案 v4：用户拍板 6 项决策（已全部落实）

用户原话决策：① PIX（不要中文名）；② 可设定但**创作者激励要匹配网站收入，不能一提现就倒贴**；③ 兑换比例可配、默认不变；④ 结算口径保持原样（点赞等默认不计入结算）；⑤ PIX 不可购买；⑥ **打赏也只能打赏代币**。

**② 的技术答案 = 偿付能力闸门（§3.4）**
- 不变式：`代币负债 L = Σ(balance+frozen) × 单价 ≤ 可用现金 C × (1 − buffer)`，buffer 默认 10%。
- `C = Σ收入(IN，已实际到账) − Σ已打款(OUT) − Σ其他支出(OUT)`。
- **两个强制校验点**：结算确认时（`L + 本次 ≤ C×(1−buffer)`，不足则拒绝并给出水位明细）、提现申请时（现金须覆盖所有未打款提现 + 本次）。
- 两道的原因：第一道保证"发出去的 PIX 有真钱对应"，第二道保证"兑现时钱还在"（防中间被挪走）。
- 前提纪律：**收入录入 = 钱已实际到账**，不是预计收入，否则水位公式是假的。录入界面要写死这句提示。
- 后台新增 `/admin/finance` 收支台账，驱动水位与前台筹资进度。

**⑥ 打赏改为站内 PIX 转账（§2.5）—— 架构级变化**
- 打赏**不再是收费点**（收费点从 3 个降为 **2 个**：站点赞助 + 容量扩容），不走支付通道、平台不经手真钱、无需审核打款。
- 计价用 PIX（预设 100/500/1000/2000 + 自定义），**前台不显示 ¥ 等值**。
- 只能用可用余额（冻结中的不能用）；不能打赏自己；不可撤回；**不产生贡献分、不计入结算**；公开打赏榜默认关闭。
- 结构上天然抗刷：**一笔打赏全站 Σ PIX 不变**（转出 = 转入），小号互打毫无收益。
- 数据模型：`TipRecord` + 两条 `CoinLedger`（`TIP_SENT` / `TIP_RECEIVED`）；`CoinReason` 增加这两个值。
- **PIX 总量只由激励池一次创造，打赏只是搬运** —— 所以「总量有限 + 真实资金背书」不会被破坏。
- 连带改动：`PaymentOrder` 删 `payeeId`/`payee`/`settledAt`，`OrderKind` 删 `DONATION`；支付订单**只有「收入进站」一个方向**，出款全在激励方案的提现口。

其他变更：代币统一叫 **PIX**（无中文别名）；`CoinAccount` 增 `lifetimeTippedOut`；后台新增 `/admin/withdrawals`（顶部常驻现金水位）；配置表增「偿付 buffer / 资金不足策略 / 打赏开关与范围 / 公开打赏榜」；验收增「偿付闸门」「打赏守恒（ΣPIX 不变、不产生贡献分）」两组反证；分期改四期（荣誉 → PIX 与结算 → 打赏与提现 → 可选）。

`payment-plan.md` 已同步：收费点 2 个、§2.2 改为「打赏已移出收费点」、`OrderKind` 删 DONATION、待拍板降为 3 项。

## 激励方案 v3：代币化 + 全量可配（用户三点要求，已并入）

用户提出：① 用"代币"而不是直接说余额多少（太刻意），**由用户自己提现**；② 下载量也切 IP；③ 所有"多少次/月"这类数值都要后台可配、不许写死。

- **三层数字**（本版核心结构）：贡献分（荣誉，只增不减，决定等级与结算权重，全站公开）/ 代币（资产，会因提现减少；命名默认「像素币 PIX」可配；前台唯一露出的资产数字）/ 元（只在提现页与后台出现）。一个数字无法同时满足"永不减少"和"能花掉"，故必须拆两层；连接点只有"每期结算 = 贡献分→代币的单向兑换"。
- **代币红线**：**只能靠贡献与打赏获得，不能购买、不能转让、不能赠送**。开充值即变资金通道（洗钱）与预付卡业务。已在文档里写成红线。
- **提现流程（用户自助，不需站长逐人打款）**：达门槛发起 → **提交即冻结代币**（防双扣）→ 后台审核 → 打款回填 → `PENDING→APPROVED→PAID`，`REJECTED` 终态并解冻。收款账号仅 adminOnly 可见、不进日志。金额与比例在提交时快照，事后改配置不影响已提交申请。
- 前台只讲代币：个人主页显示"累计获得 N 像素币"；榜单**只显示贡献分不显示代币**（避免荣誉榜变财富榜）；只有提现页谈钱；打赏按钮仍显示人民币（读者在花钱）。
- **下载量切 IP**：`DownloadCredit` 改名 `DownloadRecord`，同时承担「下载量去重」与「计分去重」——写记录成功才 `downloadCount++`，cookie `dl_done` 弃用。后果：下载量数字下降且与历史不可比（有意口径修正），建议后台注明断点日期。
- **全量可配清单（§6）**：九项分值 + 结算计入开关、月度配额、单作品上限、等级档位、代币名称/符号/比例/门槛/手续费/冷却/是否审核、分成比例、结算分门槛、最低发放额、单人封顶、结算周期、榜单周期与长度与最低等级、冻结名单。
- **验收新增「反硬编码」项**：阈值只允许出现在 `points-config.ts` 的 zod schema 默认值处，业务模块（points/download-record/settle/coin）不写字面量。
- 新增模型：`CoinAccount` / `CoinLedger` / `WithdrawalRequest` / `DownloadRecord`；新增枚举 `CoinReason` / `WithdrawStatus`；`IncentivePayout` 增 `coin` 与 `coinCredited`。
- 新增模块：`coin.ts`、`actions/withdrawal.ts`、`ip.ts`、`download-record.ts`；后台新增 `/admin/withdrawals`。
- 分期改为四期：荣誉体系 → 代币与结算 → 提现 → 可选。
- 已在文档里如实说明：**代币化只改变展示，不改变可兑付性质**，合规上仍等同余额。

## 下载计分防刷（用户要求，已并入激励方案 §4.1）

- 用户指出"下载也会被刷"，要求：**每个 IP 对每个作品只算一次 + 限制每月多少次**。
- 原方案里我按 cookie 去重（`dl_done`）已不可信 → 改为三级闸门：
  1. **主体级去重**：`DownloadCredit @@unique([resourceId, subjectKey])`，`subjectKey` = 登录 `u:<userId>` / 匿名 `ip:<ipHash>`。登录用户同时受作品×用户约束（换 IP 也再计不了）。
  2. **月度配额**：每主体每自然月默认 50 次，`(subjectKey, periodKey)` 索引计数；**超配额只停计分，绝不限制下载**。
  3. **单作品月度上限**：默认关闭（会误伤热门），改为后台异常提示。
- `refId` 改为 `dl:${subjectKey}:${resourceId}`。
- IP 哈希复用 `track/route.ts` 惯例（`sha256(ip + AUTH_SECRET)` 取 16 位），**须抽公共 `hashIp()`**（`src/lib/ip.ts`），否则两处漂移导致跨表关联不上。已同步写入 `MEMORY.md`。
- 刻意取舍：`Resource.downloadCount` **不动**（仍 cookie 口径，展示用热度指标），只管计分 → 后台「下载 1000 / 计分 200」是预期现象。待拍板项里问了是否也要切。
- 残留漏洞如实记录：换 IP 可绕闸门一（靠配额限损）；作者匿名下载自己作品识别不了。
- 新增风险项：**`x-forwarded-for` 可伪造** —— 若前面自建反代/CDN 是"追加"而非"覆盖"该头，闸门一可被绕过，上线前必须确认。
- 需新增 `monthKey()`（与 `format.ts` 的 `dayKey()` 同处）。

## 支付与收费点方案（规划，未实施）

- 产出 `payment-plan.md`；`creator-incentive-plan.md` 已补配套文档引用、§1 支付通道行、§2.1 收入来源、§10 二期依赖。
- 用户要求：接易支付 + 补收费点，但**公益站点，不能到处收钱**。
- 三条原则：① 成本分摊不是价值捕获（收的钱要能对应一张账单或一笔给作者的钱）② **不卖地位**（付费不换贡献分/等级/榜位/免审/加急）③ 够用即止（达到月成本目标后前台收起募捐入口）。
- 收费点只做 3 个：站点赞助（进成本覆盖+池）、打赏创作者（100% 直达作者、**不进池**、属代收代付）、容量扩容包（二期，前置需 per-user 配额体系——现 `UploadLimits` 是全站统一配置，**没有按用户配额**）。
- 红线（不做）：内容付费墙、下载限速/限次、付费去广告（与"广告收入喂池子"自相矛盾）、买分买等级、加急审核插队、基础社交功能切分。
- 易支付协议已核实（彩虹/好收米系）：网关 `{域名}/submit.php` 或 `/mapi.php`（JSON 返回 payurl/qrcode/urlscheme）；签名 = 除 `sign`/`sign_type`/空值外按参数名 ASCII 升序拼 `a=b&c=d`（值不 URL 编码）+ 直接拼商户密钥，MD5 取 32 位小写；回调是 **GET**，`trade_status` 仅 `TRADE_SUCCESS` 算成功，**必须返回纯文本 `success`**；查单 `api.php?act=order&pid=&key=&out_trade_no=`（**明文 key 在 URL 里**，只许服务端调用且不打日志）；另有 `act=refund` / `act=orders` / `act=query`。
- 八个必做工程点：验签（`timingSafeEqual`）、金额比对（字符串解析转分，别用 `parseFloat*100`）、`out_trade_no` 用 cuid 不可枚举、状态机幂等（重复推送不二次发货）、必回 success、主动查单兜底（回调会丢，不依赖 cron）、密钥不出服务端、`notify_url` 必须公网可达（本地要隧道）。
- 配置落点：易支付配置进 `runtime-config.ts`（后台 > env 回退，与 GitHub/SMTP 同类），**不新增 SiteSetting key**；同步改 `sanitizeRuntimeConfig` 与 `runtimeConfigIssues`。后台独立 `/admin/payment`，不塞进 runtime 页。
- 新增模型：`PaymentOrder`（`outTradeNo` 唯一 / `OrderKind` SPONSOR|DONATION|QUOTA / `OrderStatus` PENDING|PAID|CLOSED|REFUNDED）、`LedgerEntry`（收支台账驱动成本公示）。打赏在结算台作为「打赏代收」汇总进打款单（`IncentivePayout.donationFen`），同一人只打一次款。
- 验收加了反证断言：**支付成功后用户贡献分与等级必须完全不变**（守住"不卖地位"）。
- 已明确提示合规风险：易支付多为无牌照第四方支付，存在跑路/资金冻结风险，建议到账即提现、主通道优先正规渠道、保持"捐赠"定位而非销售，系统不提供合规担保。

## 激励方案 v5：`/fund` 资金池公示页 + 3 项决策按「可配」落地

用户第 6 轮 4 项决定：① 提现门槛可配 ② 分成比例取 30% ③ 打赏上限可配 ④ **在合理的地方设计一个资金池，让创作者能看到，把捐赠功能也设计在这里，展示资金池收入和支出明细**。

- 新增 §8.1 `/fund` 资金池公示页（6 区块，自上而下）：① 资金池水位（C / L + 安全线，**复用 §3.4 同一函数，禁止前后台各算一遍**）② 本月收支明细（`LedgerEntry` 三分组：收入 / 成本 / 提现打款，每笔记日期与备注）③ 各期激励结算公示（`IncentivePeriod`+`IncentivePayout`，确认期无编辑入口）④ 赞助入口 `#sponsor` ⑤ 鸣谢墙 `#thanks` ⑥ 累计数字条。
- **赞助入口从 `/sponsor` 合并进 `/fund`**：理由是「钱去哪了」与「我要给钱」必须同屏，拆两页只会多一次跳转、多一点怀疑。路由兼容处置：`/sponsor` → **301 `/fund#sponsor`**、`/sponsors` → **301 `/fund#thanks`**（页脚 / 二维码 / 历史外链不失效）。`payment-plan.md` §2.1 入口、§5 前台表、§8 分期、§9 验收已同步。
- **全页铁律**：页面上每个数字都必须能追到一条记录（`LedgerEntry` / `IncentivePeriod` / `IncentivePayout` / `WithdrawalRequest` / `PaymentOrder`），追不到的不许上页面。
- 隐私边界：**永不出现** `accountInfo`（收款账号与姓名）、任何用户可提现余额、`epayKey`、回调原始 query；`payRef` 只显后 4 位；匿名赞助对外一律匿名。**只出聚合总额，不出个人金额** —— 否则公示页变财富榜，与 §7「榜单只显示贡献分」直接冲突。
- SEO：`generateMetadata` + 进 `sitemap.ts`；**不加 `Organization`/`donation` 结构化数据**（schema 语义不稳、易被误读为募捐承诺，宁缺毋滥）。`revalidate = 300`（允许 5 分钟延迟，不为实时打满 DB）。
- 新增模块 `src/lib/fund.ts`（公开聚合**只读**，不写账）；组件 `src/components/fund/{PoolMeter,LedgerTable,SettlementList,ThanksWall}.tsx`；页面 `src/app/fund/page.tsx`。
- §6 配置增「公示」三类：流水每页条数 / 是否展开金额明细 · 鸣谢墙是否公开单笔金额 / 是否允许匿名 · 安全线是否对外显示 / 缓存秒数。默认值 20 / 展开、否 / 允许、显示 / 300。
- §11 分期：二期加 `/fund` 骨架（水位 + 收支 + 结算公示），三期收口（提现打款流水并入区块 ② + 301 落地）。**若支付一期未上线**：`LedgerEntry` 只有成本没收入 → 先由 `/admin/finance` 手工录收入兜住，赞助入口与鸣谢墙暂隐藏（不出现空区块）。
- §12 验收加 4 项：**可追溯**（抽 3 笔收入 / 3 笔成本 / 3 笔打款逐一对上，且水位数字须与 `/admin/withdrawals` 顶部**完全一致**）、**不泄露**（源码与渲染结果均不含 `accountInfo` / 用户余额 / `epayKey`）、**分页不重不漏**（游标翻页 + 空数据态有文案）、**路由兼容**（两个 301 不 404）。
- §13 由「待你拍板 3 项」改为「已锁定的决定」表：提现门槛 1000 PIX / 分成 3000 万分比（30%）/ 打赏上限 10000 PIX，**三项全部可配、无一处写死** → **已无待拍板项**。
- 已核对 `LedgerEntry` 模型位置：定义在 `payment-plan.md` §4（`kind` + `direction` IN/OUT，`@@index([periodKey, direction])`），激励方案 §3.4 与 §9 引用它，无悬空引用。
- §0 结论先行补「为什么必须有一个 `/fund` 公示页」段；改动体量更新为「6 表 + 3 枚举 + 10 模块 + 4 个前台新页 + 3 个后台页」。

**本轮未动任何代码**（`git status` 仅 `creator-incentive-plan.md` / `payment-plan.md` / `.workbuddy/memory/*`）。

## 术语澄清：分成比例 ≠ 资金池（用户提问触发，已补进 §3.1）

用户问「分成比例就是资金池是吧？」—— 不是，且文档里**有两个名字极像的「池」**，这是真实歧义，已补 §3.1「两个池必须分清」+ §8 各归一处说明。

| 名字 | 是什么 | 归零？ | 前台位置 |
|---|---|---|---|
| **激励池 P** | 本期**应发额** `floor((R+上期结转) × 分成比例)`，是账目不是钱 | 每期重算，未发完结转下期 | `/creators` 各期公示 |
| **现金池 C** | 站上**真钱** = 累计收入 − 成本 − 已打款，跨期结存 | 不归零 | `/fund` 首屏水位 |

- **分成比例 = 那个「×」**，不是池子；乘出来的那块才叫激励池。
- 切出的 30% **一刻也没离开现金池**：收入全额进 C，30% 只让负债 `L` 上升；钱真正出去只在提现打款那一刻（§3.4 分离表）。
- **新增提示（重要会计选择）**：比例是对**毛收入 R** 切的，不是对利润（收入−成本）切的。所以成本占比偏高时 `L` 会比 `C` 长得快 → 顶破安全水位 → 结算确认被闸门拒。**长期被拒 = 比例相对成本结构定高了，该下调，不是 bug。定 30% 的隐含假设是「成本长期不超过收入的 70%」。**
- §6 配置项改名：「分成比例（万分比）」→ **「创作者分成比例」（万分比，对毛收入；定义见 §3.1）**。
- 已按 §3.1 的图（`show_widget`）向用户解释：账目口径链 收入 R ×30% → 激励池 P → 折算 → PIX 负债 L；真钱口径链 收入 R 全额 → 现金池 C → 提现出账；虚线表示 L 只在提现时从 C 扣。

## 账目修复：`carryInFen` 被二次打折（用户追问「30% 分给创作者？」时发现，已改）

用户继续追问「就是网站收入拿总的 30% 来分给创作者？」，复核公式链时发现**原公式是错的**：

```
❌ 原：P = floor((R + 上期结转) × rate / 10000)
✅ 改：P = floor(R × rate / 10000) + carryInFen      // 池余全额结转，不再乘比例
```

- **为什么是错**：`carryOut` 是**已经按比例切好、已经指定给创作者**的钱。下期再乘一次比例 = 把其中 `1−rate` 悄悄收归站里。数值反例：收入 1000、结转 20、rate 30% → 错算 `floor(1020×0.3)=306`（结转只"生效"6 元），对算 `300+20=320`。**消失的 14 元既没进 `poolFen`，也没记成站内收入 —— 无账可查**，§12 跨期守恒必然失败。
- 规则：**分成比例只作用于本期新增收入；池余全额结转、原样回流。**
- 连带澄清：§3.4 的「上期结转**现金**」是现金池 C 的定义项，与 `carryInFen` 不是同一个数字（原文措辞容易混，已加 ⚠️ 提示）。
- 改动落点：§3.1 计算链 + 两个池对照表 + 新增「为什么 carryIn 不再乘比例」反例表；§3.2 结转句；§0 资金闭环图；**§12 新增验收项「跨期守恒」**（断言 `poolFen === floor(收入×rate/10000) + carryIn` 而非 `floor((收入+carryIn)×rate/10000)`，专防此写法回归）。

**教训（可复用）**：涉及"按比例切 + 跨期结转"的资金模型，必须逐项问「每个加数是否都该再乘一次比例」；结转项几乎永远**不该**再乘 —— 它已经是上次乘完的产物。这类错误在单价上量不出来（差几毛钱），只在跨期账目对平（`Σpayouts + carryOut == pool`）时才暴露。

## 其他

- `MEMORY.md` 超限，已压缩（7.7k → 5.0k 字符）：删掉实测数字与踩坑经过，保留规则本体。

## 开始落地创作者激励（用户：「开始完成这些功能，并将分成比例提高到60」）

分成比例默认 30% → **60%**（`points-config.ts` 的 `ratePermille: 6000`、计划 §0/§3.1/§6/§13 同步）。60% 对**毛收入**切 + 10% 安全水位 ⇒ 只剩 ~30% 覆盖服务器与运营成本；管理层已把这条风险写进后台配置页的实时提示（超 100% 时转红字），不是隐藏假设。

### 已完成（Task 1–10）

| # | 产物 | 要点 |
|---|---|---|
| 1 | `prisma/schema.prisma` | +10 模型 / +4 枚举（UserPoint / PointLog / CoinAccount / CoinLedger / TipRecord / WithdrawalRequest / DownloadRecord / RevenueEntry / IncentivePeriod / IncentivePayout） |
| 2 | `prisma/migrations/0006_creator_incentive.sql` | `migrate diff --from-empty` 生成后转幂等（`DO $$ duplicate_object` / `CREATE TABLE IF NOT EXISTS`）。**尚未对任何库执行** |
| 3 | `src/lib/points-config.ts` | 全部阈值唯一落点；`satisfies Record<PointReason, number>` 编译期护栏；`safeIncentive()` 整份校验 |
| 4 | `src/lib/ip.ts` | IP 哈希唯一实现，`track/route.ts` 去掉内联副本（顺带修了 `x-real-ip` 回退缺失导致的单桶限流） |
| 5 | `src/lib/points.ts` | `awardPoints` 唯一写入口 + `interactionRefId`（**必须把触发者编进 refId**，否则全站对同一作品只加一次分） |
| 6 | `src/lib/download-record.ts` | 主体去重（`u:<id>` / `ip:<hash>`）+ 月配额（**只停计分，绝不拦下载**） |
| 7 | LevelBadge / `u/[username]` / `/creators` / `/creators/me` | 荣誉榜只展示贡献分，不展示 PIX |
| 8 | 首页+侧栏 creators 板块加 `sort`/`period` | 见下 |
| 9 | `/admin/incentive` + `actions/incentive.ts` + `IncentiveManager` + `incentive-parts` | 整份替换式保存（WYSIWYG），服务端 zod 权威校验 |
| 10 | `prisma/backfill-points.ts` + `npm run points:backfill` | **默认预演，`--apply` 才写库**；refId 与线上同构 ⇒ 幂等 |

### 本轮新增的可复用规则

**① creators 板块排序口径（`sort` / `period`）—— 默认值即兼容红线**
`getTopCreators(limit, sort="followers", period="all")`：**默认值绝不能改**，存量配置只有 `count`，改默认会让不改后台的现网排序被动变化。
返回值的 `metric` 是**驱动本次排名的那个数**，四组合语义不同（followers+week = 窗口内**新增关注数**，不是累计粉丝数），所以展示必须走同一个 `creatorMetaText(resources, metric, sort, period)`（落在 `format.ts`），写死「粉丝」会把贡献分榜说成粉丝数。
后台两个编辑器（`home-admin/SectionEditor`、`site-admin/WidgetEditor`）都要加 `sort`+`period` 控件，**漏一个就会出现「配置能存但界面调不了」**。

**② TypeScript 联合类型的对象字面量赋值陷阱（真实踩到）**
给 `HomeSectionConfig` 的 creators 分支加 `sort` 后，`DefaultSectionSpec[]` 与 `buildConfig()` 双双报错：`Type '"followers"' is not assignable to type '"latest"|"popular"|"downloads"'` / `Type 'number' is not assignable to type 'never'`。
原因不是写错了，而是**联合类型里两个成员共用了 `sort` 这个名字**（list 的 `sort` 是 `latest|popular|downloads`），且 `stats` 成员是 `Record<string, never>`（能接任何对象，最容易成为报错对象）→ TS 的「最佳匹配」挑错成员。修法是给 creators 分支的 `sort` 一个**独有字面量集合**（本案例是补齐 `count + sort: "followers"|"points" + period` 后按分支精确匹配）。**下次往这类配置联合加字段，要预期这个报错并一次把 schema / 联合类型 / 默认值 / 两个编辑器四处同时补齐。**

**③ `UserPoint.frozen` 列目前是死列**
`awardPoints` 只看配置里的 `risk.frozenUserIds`，不读 `UserPoint.frozen`；`getContributionSummary` 读列不读配置。**两处口径不一致**，且没有任何代码写该列。要么后续统一到配置（删列），要么让 `awardPoints` 同时认列。已记为待办，本轮没动。

**④ 后台配置页的「不生效字段」要真 disabled，别用遮罩**
`[&_*]:pointer-events-none` 这类遮罩只是视觉禁用，键盘 / 屏幕阅读器仍能进入。给 `NumRow` 加 `disabled` 直达 `<input>`，并在 hint 里写清「需先打开上面哪个开关」。

### 验证（本轮实际执行）

| 命令 | 结果 |
|---|---|
| `node node_modules/typescript/bin/tsc --noEmit` | 0 错误 |
| `eslint src` | **仍是基线 3 条 warning，零新增** |
| `eslint src prisma` | prisma/ 只有 seed-bulk / seed-extra 的存量 warning，新增 `backfill-points.ts` 干净 |
| `next build` | 编译成功，47 静态页；`/creators`、`/creators/me`、`/admin/incentive` 均在路由表（全站路由都是 ƒ 动态） |
| `tsx prisma/backfill-points.ts`（预演，只读） | **实跑过**：收集到 **157 条候选事件**，随后卡在 `pointLog.findMany` 报 `P2021 表 public.PointLog 不存在` → 反证迁移确实未执行 |

- 顺带把这次失败变成可操作提示：回填脚本加了 `preflight()`（查 `pg_class` 里 `UserPoint`/`PointLog` 是否存在），缺表就打印「请先执行 0006 迁移」并退 1，不再甩 Prisma 堆栈。复跑已确认提示生效。
- 这次实跑同时验证了 tsx + `@/*` 别名 + Prisma 自动读 `.env` + 采集侧 5 条查询全部可用 —— 回填逻辑本身没问题，**唯一阻塞是迁移**。

- **`npm run build` 在本机不可用**：bash shim 里 `npm` 不通（127），改用 `node node_modules/prisma/build/index.js generate` + `node node_modules/next/dist/bin/next build` 两步代替。
- **构建不触库**：全站路由都是按需渲染，所以 `0006` 迁移未执行也不影响 build；但 `/creators`、`/admin/incentive` 等**运行时必须先把迁移跑上去**，否则 `UserPoint/PointLog` 不存在会直接 500。

### 未完成 / 需用户决策

1. **迁移未执行**：`prisma/migrations/0006_creator_incentive.sql` 只生成未跑。已用只读预演反证（见上）。需要用户确认目标库后再执行（写操作不擅自做）。
   执行后即可跑 `npm run points:backfill`（默认预演）→ 看 157 条的数字 → 再加 `--apply`。
2. **阶段 2–4 未开工**：PIX 账本与结算引擎、打赏与提现、`/fund` 公示页（`disclosure` 一组配置已在后台可见但标注「结算功能上线后生效」）。
3. `UserPoint.frozen` 死列口径不一致（见上「③」），本轮没动。

## 阶段 2–4 落地完成：PIX / 结算 / 打赏 / 提现 / 支付

用户指令「统一到配置吧，并且完成后续阶段」已全部完成。① `UserPoint.frozen` 死列**已删**，评分冻结统一到 `cfg.risk.frozenUserIds`（单一事实来源，`awardPoints` 与 `getContributionSummary` 不再两套口径）；② `creator-incentive-plan.md` §11 二/三/四期 + `payment-plan.md` 支付一期代码全部落地。

### 新增产物（均为正式交付物，非探针）

| 层 | 文件 |
|---|---|
| 库 | `src/lib/coin.ts`（applyCoin/creditCoin/debitCoin/freezeFordrawal/refundWithdrawal/markWithdrawalPaid/getSolvency/checkSettlementGate/checkWithdrawGate）、`settle.ts`（buildDraft/confirmPeriod/periodPayouts/settlementAnomalies/payoutCounts）、`settle-allocate.ts`、`payment.ts`、`epay.ts`、`ledger.ts`、`fund.ts`、`tip.ts`、`payment-config.ts`、`payment-settings.ts`、`ip.ts`、`download-record.ts` |
| actions | `adjust.ts`（人工调整）、`settlement.ts`、`withdrawal.ts`、`payment.ts`、`tip.ts`、`incentive.ts` |
| 前台 | `/fund`（6 区块 + `#sponsor`/`#thanks`/`#settlements`）、`/creators`（加激励公示层）、`/creators/me`、`/me/coins`、`/pay/result`；`/sponsor`→308 `/fund#sponsor`、`/sponsors`→308 `/fund#thanks` |
| 后台 | `/admin/finance`、`/admin/settlement`、`/admin/withdrawals`、`/admin/payment`、`/admin/incentive`（挂 `AdjustPanel`） |
| API | `/api/pay/{create,notify,status,refund}`、`/api/settle/export`（CSV） |
| 迁移 | `prisma/migrations/0006_creator_incentive.sql`、`0007_payment.sql`（幂等） |
| 测试 | `_test/incentive-conserve.ts` + `npm run test:incentive`（75 断言） |

### 本轮验证（实际执行）

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| `eslint src` | 仅基线 3 条 warning，零新增 |
| `next build` | 成功，全部激励路由为按需渲染 ƒ |
| `npm run test:incentive` | **75/75 通过**（A 类纯函数 + B 类 DB 不变量；无库时 B 类 SKIP） |
| 后台 5 页带 admin cookie 渲染 | 全部 200，含预期区块文案 |
| 后台 5 页无 cookie | 全部 307 → `/login?callbackUrl=` |
| `/api/settle/export` 分支 | 无 cookie **403** / 期号非法 **400** / 期不存在 **404** / 草稿期 **400** |
| 导出 happy path（造 `1999-01` 临时期 + finally 清理） | **17/17 通过**：200、`text/csv; charset=utf-8`、`attachment` 含期号、BOM、CRLF、表头、金额换算、封顶标记、**公式注入防护 + 引号/逗号/换行转义**；清理后 `period=0 payout=0 nameRestored=true` |
| 支付接口边界 | 游客赞助未配通道 → **303 `/fund?pay_error=…`**（优雅降级）；金额 0/非数字/超上限 → 400；跨站 Origin → 403；查单不存在 → 404；`GET /pay/status` → 405 |
| 密钥不出服务端（活体响应） | `GET /api/pay/create` 公开配置**不含** `key`/`pid`/`url` |

### 本轮新增的可复用规则

**① 校验 BOM 不能用 `fetch().text()`** —— WHATWG 规范下 `text()` 会**剥掉** U+FEFF，断言 `charCodeAt(0)===0xFEFF` 必然假红灯。必须 `Buffer.from(await r.arrayBuffer())` 查原始字节 `EF BB BF`。同类：任何"响应体首字符"类断言都要先想清平台有没有替你规范化。

**② `permilleText(n)` 会去掉无意义小数位**（`6000 → "60%"`，`points-config.ts:380` 有注释约定），不是 `"60.00%"`。写导出/公示断言时别按两位小数写。

**③ Next.js dev server 有目录级互斥锁** —— 同目录第二个 `next dev` 会打印 `⨯ Another next dev server is already running`（含已运行实例的 PID / 端口 / 日志路径）**并退出**。所以「探测旧实例失败」不等于「没有实例」：本机 3100 那个一直在跑，对新端口 3200 的探测 ECONNRESET 是误导信号。**先读日志再决定重启。**

**④ 放在 `%TEMP%` 的脚本 require 解析基于脚本目录**，取不到仓库依赖。解法：`createRequire("E:/project/pixel_hub/package.json")` 再 require。

**⑤ `/api/pay/create` 收的是 `formData` 不是 JSON**（打 JSON 得 400「请求格式不正确」，非鉴权问题）；**未登录可赞助是有意的**（`allowGuest`，注释在 `create/route.ts:20-23`），别当成漏鉴权去"修"。

**⑥ 本库（dev）激励表全部 0 行**（Coin*/Tip/Withdrawal/Payment/Revenue/Period/Payout 皆 0），`IncentivePeriod` 无行 → 导出 happy path 只能靠临时造数验证；0006/0007 迁移在生产库是否执行取决于生产走 `migrate` 还是 `db push`（本机是用 `db push` 建的，没有 `_prisma_migrations` 表）。

**⑦ 守卫分支要用"活体"验证，别只看代码** —— 本次导出接口 4 个分支、支付 8 个边界全部实测；活体响应还能顺带证明密钥投影真的没漏（比读 `publicPaymentConfig` 源码更有力）。

### 仍未覆盖（证据限制）

- **未做视觉验收**：本机无浏览器 / 无 msedge CDP，计划 §14 要求的 320/375/390/430/768/1024/1440px × 浅色/暗色截图**无法产出**，已用 dev server + fetch + 正则探针替代（只能证明"渲染出预期区块"，不能证明布局与对比度）。
- 迁移未在生产库执行（需用户授权）；`0006/0007` 未对任何库执行过，本机表来自 `db push`。
- 无真实支付通道（`ready:false`），易支付验签/回调/查单/退款全链路**未对真实上游跑通**。

## 前台文案纠偏：配置项说明不该出现在用户页（用户指令）

用户原话：「前台用用户看的不要写那么多关于配置项的提示」。上一轮我把「透明化」理解成了"把配置口径写出来"，在对外页面塞了大量后台语汇，方向错了 —— 透明化的对象是**账目数字**，不是**配置旋钮**。

改动 7 文件（净 −19 行）：

| 文件 | 删掉的东西 |
|---|---|
| `components/fund/PoolMeter.tsx` | 「（安全水位 10% 可用）」；水位说明由 3 句压成 2 句 |
| `components/coins/WithdrawForm.tsx` | 「折算比例 100 PIX/元，以提交时的比例为准」；四句长句拆成"一句后果 + 三条约束列表" |
| `app/me/coins/page.tsx` | 提现门槛折合 ¥ 的复述（与 WithdrawForm 重复）、代币流水的重复 info 块（与页首说明重复）、随之无用的 `coinToFen`/`minFen`/`Info` 导入 |
| `app/creators/me/page.tsx` | 「门槛由站长配置。」、贡献流水的"为什么点赞不计分"设计理由、月榜口径对比结算期 |
| `app/creators/page.tsx` | 公示说明里的**反引号**（会原样渲染）、「兑换比例与门槛见资金公示页」、设计理由「同一个数字只在一处出现才对得上账」 |
| `app/fund/page.tsx` | 内部术语「**偿付闸门**」（改说"顺延到收入到账后再处理"）、「不提供任何编辑入口 —— 已公示的数字必须永远可复算」、对外提「密钥」 |
| `components/fund/SettlementList.tsx` | 「结转不参与分成」（保留括号，纯标点） |

- 判断准绳：**这条信息会不会改变用户的行为？** 门槛/手续费/冷却会 → 留；折算比例（旁边已经算出金额）、"以提交时的比例为准"、设计理由 → 删。
- 内部术语泄漏是这类问题的重灾区：`偿付闸门`、`台账口径`、`冻结名单` 这些是给管理员看的词，前台要翻译成人话。

### 验证（本轮实际执行）

| 命令 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| `eslint src` | 仅基线 3 条 warning，零新增 |
| dev server 探针 `/fund` + `/creators` | 200，且断言旧文案字符串**全部消失**、新文案在位（2/2） |
| `/me/coins` + `/creators/me` 无凭据 | 307 跳登录 = 模块编译通过、鉴权分支正常；**渲染结果未肉眼验证**（本机无浏览器，且 JWT 会话要重新铸造才带得动） |

**踩坑**：编辑后立刻发请求会撞上 dev server 重编译，`/fund` 与首页都返回 **500**（瞬时假红灯）。等编译完再请求即 200。别急着当 bug 修 —— 先重试一次、或看 `.next/dev/logs/`。

**踩坑**：用 Edit 改一行做"加括号"这种微调时，把行尾换行一起吞进了 old_string，导致相邻两行被拼成一行（`...carryInFen)}              </span>`）。**行尾换行不要放进 old_string**；改完立刻 Read 回看那几行。

