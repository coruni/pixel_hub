# 2026-09-08

## /upload 编辑器升级为所见即所得 Markdown（Milkdown Crepe）

- 用户新增依赖 `@milkdown/crepe@7.22.1`（ProseMirror WYSIWYG，全 npm 本地资源、vanilla 实现，排除 vditor 的 CDN 运行时依赖与 bytemd 的非 WYSIWYG 方案）。
- 新建 `src/components/rte/MdEditor.tsx`：`new Crepe({root, defaultValue, features, featureConfigs})`，禁用 Latex/AI/CodeMirror，`markdownUpdated` 回调透传 onChange；StrictMode 双挂载用 create→destroy 生命周期处理；`onChangeRef` 在 useEffect 内更新（react-hooks/refs 禁止 render 期写 ref）。
- `UploadWizard.tsx`：textarea 换为 `<MdEditor>` + `<input type="hidden" name="description">`；原生 required 移除，靠服务端 `resource-fields.ts` 的 `min(10)` 兜底。IMAGE minHeight 12rem / ARTICLE 24rem。
- 顶部与各分区提示文案按 de-ai-flavor 原则精简（大白话、去空壳）。

## 用户反馈的编辑器三问题（修复）

- 缺边框 → wrapper 加 `rounded-none border border-brand-200 bg-surface`。
- 内边距太大 → crepe reset.css 默认 `.milkdown .ProseMirror { padding: 60px 120px }` (0,2,0)，用 `.md-editor .milkdown .ProseMirror { padding: 12px 14px; min-height: inherit }` (0,3,0) 覆盖（CSS 优先级按特异性，与加载顺序无关）。
- 可编辑区域太小 → 随 padding 修复 + min-height inherit 链解决。

## 主题映射

- globals.css 末尾追加 `.md-editor .milkdown` 作用域：20 个 `--crepe-color-*` 全部映射到站点 token（`--surface`/`--foreground`/`--md-muted`/`--brand-*`），暗色经 token 链自动跟随；`border-radius: 0 !important` 保持全站直角像素语言；`--crepe-base-font-size: 14px` + 字体 inherit。
- crepe 的 frame-dark.css 因无条件覆盖站点色不可引入，仅手动映射。

## 验收结论

- tsc ✅ / lint ✅（8 个存量 warning，无新增）/ `npm run build` 后台执行。
- 浏览器截图验收（浅色 ed-final2.png + 暗色 ed-dark.png）：`# 标题`→h1、`**加粗**`→粗体实时渲染；边框/紧凑内边距/大可编辑区确认；暗色 token 链自动跟随无白底残留。
- 浏览器坑（复用价值）：agent-browser daemon 在 Bash 工具调用间重置，登录+导航+操作+截图必须单条命令链；登录字段是 `identifier`/`password`（Auth.js）；ProseMirror 要用 `focus` 不响应 click；触发 Markdown input rules 需 `keyboard type` 真实键事件（execCommand 会绕过）。

## 待办

- 未提交改动：`MdEditor.tsx`(新)、`UploadWizard.tsx`、`wizard-sections.tsx`、`globals.css`、`package.json`、`package-lock.json`。等用户推送指令（当前分支 master，ahead 32 / behind 4，推送前需先处理分叉）。
- 已知边界：邮件模板仍用 env 站点名；编辑器 placeholder 固定文案不随资源类型切换。

## 第二轮：图片信息瘦身 + 编辑器全屏 + 移动端快捷块适配

- **图片信息删字段**：`wizard-sections.tsx` ImageSection 删掉生成工具(aiTool)/模型(aiModel)/授权(license)/素材来源(sourceNote) 四个 input；`ResourceEditForm.tsx` 后台改稿同步删传参。字段链路（meta.ts schema / resource.ts、_resource-edit.ts action 解析 / 详情页展示）全部 optional，旧数据保留在 meta 只是 UI 不再收集与展示。checkbox 声明（由 AI 生成/本人原创）保留。
- **编辑器全屏编写**：`MdEditor.tsx` 重构——crepe root 从 wrapper 改为内嵌 host div，wrapper 顶部加极简工具条（border-b + 右侧全屏按钮，lucide Maximize2/Minimize2，focus-visible 环）；`data-fullscreen` 时 fixed inset-0 z-50 flex column，滚动交给 .milkdown（`overflow-y: auto`）；inline minHeight 全屏时移除（否则压过 CSS）；Esc 退出 + body overflow 锁定随 effect 清理。
- **移动端快捷块溢出根因**（读 crepe/plugin-slash/plugin-block 源码确认）：slash menu = floating-ui `placement: bottom-start` + `[flip(), offset(10)]` 无 shift；block handle = `placement: left` + `offset(16)`，flip 只翻上下不救左右 → handle 位于编辑区左缘外 ~70px 窄屏必出屏。修复（globals.css `@media (max-width:767px)`）：block handle 触摸端直接隐藏（输入 `/` 仍可快捷添加）；slash menu `max-width: calc(100vw-24px)` + `max-height: min(60vh,420px)` + 菜单层接管滚动。
- 验证：tsc ✅ / lint 0 error 8 存量 warning / build。用户明确本轮不需要浏览器验收。

## 第三轮：全屏按钮改浮动图标 + 全屏补 padding + 编辑器图片真实上传

- 全屏按钮按用户反馈改为：无工具条、无文字，图标按钮 absolute right-1.5 top-1.5 浮在编辑器右上（站点式直角边框小方块）；host 加 `md-editor-host` 类，全屏 CSS 从 `> div:last-child` 改类选择器（否则命中按钮）。
- 全屏补回 padding：块操作柄定位在内容左缘外约 82px（floating-ui left + offset 16 + 柄宽 66），全屏铺满视口无缓冲时被挤出屏幕 → `[data-fullscreen] .ProseMirror { padding: 48px 120px }`，移动端全屏（柄已隐藏）16px。
- 编辑器图片上传接真实接口：crepe `ImageBlock` 配置 `onUpload`（源码确认 inline/block 均回落到此配置）→ POST `/api/upload?max=1`（与 MediaPicker 同一接口，媒体库管理一致）→ 返回 `files[0]` 取 `origUrl ?? bigUrl ?? thumbUrl` 回填 Markdown。
- 编译环境坑（勿重复踩）：后台 `npm run build` 会触发沙箱 safe-delete guard（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`，Turbopack 清理 `.next` 被 node-safe-delete-shim 拦截）；前台 build 曾成功但 Prisma DLL EPERM 复现（残余文件锁）。用户明确指示「不要管编译」，以 tsc + lint 为准。
- 移动端全屏跟进：`[data-fullscreen]` 下左 padding 88px（≥ 柄 82px 空间）+ `display: flex !important` 恢复 handle（特异性 (0,4,0) > 隐藏规则 (0,3,0)，非全屏仍隐藏），右侧/上下 24/40px，375px 视口内容区 263px。
- 移动端 slash 菜单滚不动修复：滚动回归官方单一滚动层 `menu-groups`（max-height min(52vh,420px)），外层 `.milkdown-slash-menu` 只限宽不 overflow——双层滚动容器在移动端会吞触摸手势。menu-groups 挂载点确认：plugin-slash append 到 `view.dom.parentElement`（.milkdown 内）。
- 编辑器图片上传报 `chevereto 上传失败: HTTP 400` 根因修复（`src/lib/storage/chevereto.ts`）：`form.append("source", new Blob([bytes]))` 缺文件名与 Content-Type，PHP 端 chevereto 识别不了文件报 400 → 改为从 key 扩展名推断 mime（key 由 processImage 生成必带扩展名）+ `append("source", blob, "image.<ext>")`；`!res.ok` 时读取响应 body 的 `error.message` 带进错误消息（原先只抛状态码，调试信息全丢）。全站图片上传唯一路径 `/api/upload` → processImage → saveFile → driver，向导与编辑器同链路，驱动修复对两边同时生效。
- /upload 提示二次精简（de-ai-flavor）：顶部提示删类型罗列留「免审用户直接上架，其他人等审核通过。」；Markdown 语法提示整行删（所见即所得+placeholder+服务端校验已覆盖）；游戏区合规提示去括号罗列；图片附件提示压成「可放原画集或网盘链接，没有就跳过。」；删「转载注明来源，AI 生成如实标注」与「附件会列在文章底部」（勾选项/SectionTitle tail 已表达）。
- 文章媒体收敛为 1 张封面（其余插图放正文/编辑器上传）：新增 `ARTICLE_MEDIA_MAX = 1`（upload-config.ts），删除 `articleImageMaxCount` 配置全链路（UploadLimits 类型、默认值、clamp、uploads.ts 更新入口、UploadLimitsManager UI）；发布校验 resource.ts + 编辑校验 _resource-edit.ts 均「文章只需 1 张封面图，其余插图放正文里」；UploadWizard / ResourceEditForm / MediaPicker（文章标题「插图」→「封面」、`multiple={maxCount>1}`）客户端同步。注意：旧文章若有多张插图，编辑保存会被新校验拦截，需删到 1 张。
- Footer 组件化 + rules 页扩充：新建 `src/components/layout/Footer.tsx`（三栏：品牌/探索/社区 + 版权底行，链接数据驱动，`SITE_ICP`/`CONTACT_EMAIL` env 未配置自动隐藏），layout.tsx 内联 footer 替换为 `<Footer name={name} />`（Navbar 同款 bg-surface + border-brand-200 + max-w-7xl）。rules 页新增「资源使用与下载」「免责声明」两节（共 6 节）+ 锚点目录 chip（BTN_FILTER 风格）+ `scroll-mt-20` 锚点偏移 + 最后更新日期。
- Footer 按用户反馈返工：**恢复原单行简洁样式**（三栏版废弃），不要 .env —— `icp`/`contactEmail` 进 SEO 后台配置（seo-config schema + sanitizeSeo trim/slice 60/200 + SeoManager「站点信息」区块两输入框 + updateSeoConfigAction 走 schema 整体 parse 无需改），Footer props { name, icp, contactEmail }，未配置条件隐藏；有配置时追加第二行 `© 年份 · 联系我们 · 备案链接`。**教训：站点可运营配置一律进后台配置体系，不用 env；UI 改动先确认用户要的样式基准再动手。**
- 内容页后台化（rules/terms/privacy）：新建 `src/lib/doc-config.ts`（DOC_PAGES 元数据 + DEFAULT_RULES_MD/DEFAULT_TERMS_MD/DEFAULT_PRIVACY_MD 内置默认 + getDocMarkdown cache / getDocWithMeta，SiteSetting key `doc:<page>` 直存 Markdown）；`actions/docs.ts` saveDocAction/resetDocAction（isDocKey 白名单防任意 key 写入、MAX 100k、adminOnly+audit EDIT_DOC_PAGE、revalidate 三前台页+后台）；前台 `DocPageView`（图标头 + md-body Markdown 渲染）共用，`/rules` 重写、新增 `/terms` `/privacy`；后台 `/admin/docs?doc=rules|terms|privacy`（searchParams 切换 + 顶部 tab 链接）+ `DocsEditor`（MdEditor + 保存/恢复默认，父级 key={page} 强制重挂载防 defaultValue 不刷新）；admin layout tabs 加「内容页面」（FileText 图标，adminOnly）。页脚 `footerText`（seo-config 120 字，空回退默认「分享与发现 · 请遵守平台规则…」）+ 页脚文档链接组（社区规则·用户协议·隐私协议）。











## .env 运营配置后台化（站点配置）

- 新建 `src/lib/runtime-config.ts`（key=site-runtime）：GitHub OAuth / 存储（driver+chevereto+s3 全项）/ SMTP 邮件的 schema+sanitize+cache 读取；生效值解析函数集中「后台配置 > env 回退」优先级（site-config.ts 名字已被站点外观主题占用）
- `auth.ts` 改 NextAuth v5 函数式懒初始化 `NextAuth(async () => config)`，GitHub provider 每次请求从后台配置组装；login/settings/connections 三处 githubEnabled 联动改读配置
- `mailer.ts`/`mail-notify.ts`：smtpCfg() 读运行配置，emailNotifyEnabled 改 async（调用点仅内部）
- 存储层：index.ts DRIVER 常量→getDriver() 动态分发（driver 导出删除，无人引用）；s3.ts 顶层常量函数化+client 按配置签名缓存重建；chevereto base/apiKey 走配置；url.ts 加 setS3Runtime 同步镜像（client-safe 纯函数的 server 配置注入方案）；/api/dl 白名单基址动态读
- 后台：`/admin/runtime`「站点配置」页 + RuntimeConfigManager 三组表单（secret 字段 password 类型）+ updateRuntimeConfigAction（adminOnly+乐观锁+audit EDIT_RUNTIME_CONFIG）；tabs 用 Settings2 图标
- 顺带：seo-config 加 siteLogo 字段（站点信息区）后台化 NEXT_PUBLIC_SITE_LOGO，Navbar 读 seo.siteLogo 回退 env
- 教训：**同一文件多个 Edit 并行会竞态丢改动**（SeoManager 三处 edit 并行，两处被覆盖丢失，tsc 抓出后串行补齐）——同文件编辑必须串行
- 边界：DATABASE_URL/AUTH_SECRET/AUTH_URL/SITE_URL 基础设施留 env；Graph 云附件凭据按注释设计意图留 env；后台留空的项回退读旧 env，迁移期平滑

## 后台页面合并 + 宽度修复

- 「站点配置」(/admin/runtime) 并入 SEO 配置：页内分「运行配置」「SEO 配置」两节（RuntimeConfigManager + SeoManager）；删除 /admin/seo，actions/seo.ts revalidatePath 改 /admin/runtime
- 「站点布局」(/admin/site) 并入首页布局：页内分「首页布局」（HomeManager）「全站布局」（SiteLayoutManager）两节；删除 /admin/home，actions/home.ts homeRevalidate 改 /admin/site；SiteLayoutManager「首页板块流在首页布局页管理」文案去链接改指本页上方
- admin/layout.tsx tabs 删「首页布局」「SEO 配置」两项；AdminTabs 删 LayoutTemplate/SearchCheck 图标映射
- 宽度修复：RuntimeConfigManager / SeoManager 删 max-w-2xl，配置块铺满后台内容区
- 坑：删除页面后 tsc 报 .next/types/validator.ts 引用旧路由——过期构建产物，rm -rf .next/types 即清

## 合并页改 tab 切换

- 新建通用 `src/components/admin/SubTabs.tsx`（client）：页内分区 tab，两节一次性渲染、hidden 显隐切换——表单/拖拽未保存状态在切换间保留；样式对齐 SiteLayoutManager 的 border-b-2 下边线式 tab
- `/admin/runtime`「站点配置」：tab = 运行配置 | SEO 配置；`/admin/site`「站点布局」：tab = 首页布局 | 全站布局；各节说明移入对应 panel
- 用 hidden 而非条件渲染的原因：URL/条件渲染切 tab 会 remount 组件丢表单状态

- 页脚 footerText 为空不再显示内置默认文案（删 DEFAULT_FOOTER_TEXT），SeoManager placeholder 与 seo-config 注释同步「留空不显示」

## 注册邮箱验证码 + 邮件 HTML 模板（补齐）

- 新建 `mail-template.ts`：renderMailHtml（全内联样式、品牌头 #d97757 色条、醒目大字段 highlight、按钮、脚注；text 参数保留纯文本兜底）；password-reset 与 mail-notify 两个调用点换上模板
- 新建 `register-code.ts`：内存 TTL 验证码存储（10 分钟 / 5 次尝试 / crypto.randomInt 6 位 / 验证成功即销毁 / MAX_KEYS 2000 防膨胀）；emailCodeRequired() = 后台开关 && smtpConfigured()
- 新建 `actions/register-code.ts`：sendRegisterCodeAction（IP 每小时 10 次 + 单邮箱每分钟 1 次；已注册拒绝；未配 SMTP 报错兜底）
- registerAction 接入校验（6 位格式 + verifyRegisterCode，fieldErrors 加 code）；register-form 加验证码栏（发送按钮 60s 倒计时，ref 读 email 值 FormData）；register/page.tsx server 端算 codeRequired传入——SMTP 未配置时验证码栏不渲染，开箱即用
- runtime-config 加 emailCodeRequired 开关（默认 false，后台 SMTP 区开关控制）
- 坑：mail-template 加 highlight 时 Edit 把 `const button =` 吞了（old_string只匹配到变量名行），tsc 能抓 —— 复合行编辑 old_string 要带上下文
- 邮件模板返工为站点像素风格：全直角（站点 border-radius:0 语言）、外底 brand-100 #faece3、卡片白底 2px brand-200 边 + 4px 4px 0 硬阴影、头部 3px brand-500 色条 + 方块点缀、按钮 brand-500 底 2px brand-700 边 + 3px 3px 0 brand-800 阴影、验证码框 brand-50 底 2px brand-500 边直角大字。教训：邮件模板风格必须从站点 CSS token 取值，不能套「通用干净邮件」

## commit 分组提交完成（2026-09-08）
累积改动按 4 个逻辑提交落库（master 分支）：
- 43e8725 Markdown 编辑器全屏与图片上传接入真实接口；文章插图收敛为单张封面
- d00d3d6 新增页脚组件与规则/用户协议/隐私协议内容页，后台可编辑
- 80e9210 运营配置后台化：GitHub OAuth/存储/SMTP 迁入后台「站点配置」
- b0fc968 注册新增邮箱验证码校验；邮件统一像素风 HTML 模板
刻意未提交：.workbuddy/tmp/、overview.md。用户未说推送，未主动 push。
提交前 tsc --noEmit 通过。

## Graph 凭据后台化（2026-09-08 下午）
用户指出云盘配置没进后台。改造：
- runtime-config schema 加 graph 五字段（tenant/clientId/clientSecret/endpoint/scope），生效值函数带 GRAPH_* env 回退
- onedrive.ts graphCreds/graphEnabled 改 async 读 site-runtime；ticketSecret→signTicket→create/readDriveUploadTicket 全链 async 化（外部 3 调用点均在 async 上下文）
- DriveManager 顶部加「Microsoft Graph 凭据」配置卡（secret 用 password；key={configVersion} 保存后 remount 同步）；旧"到 .env 补全"提示条改指页内配置
- 关键修复：RuntimeConfigManager form 是显式字段列表，直接提交会把 graph 字段抹空——改为提交 {...config, ...form} 全量合并（两页互不覆盖）；updateRuntimeConfigAction revalidate 加 /admin/drives
- tsc + eslint 零错误。未 commit（用户未指示）

## 上传限制页卡片布局修复（2026-09-08）
UploadLimitsManager 三卡在 xl 两列 grid（1.1fr/0.9fr）中因 DOM 顺序（图片限制→数量限制[col-span-2]→附件格式）导致自动放置错位：数量卡被推到 row2 整行，附件格式卡孤落 row3-col1 仅占 55% 宽。修复：图片限制卡加 xl:col-start-1 xl:row-start-1、附件格式卡加 xl:col-start-2 xl:row-start-1（xl 并排、数量卡自动落 row2 全宽）；小屏单列不变。最小 diff（2 处 className）。tsc/eslint 通过，未 commit。

## Gallery 缩略图同步修复（2026-09-08）
Gallery.tsx 缩略图条为手写 overflow-x-auto flex（非 Swiper thumbs 模块），主图经 swiper 拖动/键盘/箭头切换后高亮缩略图可能停在可视区外。修复：activeThumbRef 绑在选中缩略图（ref={i===index?activeThumbRef:undefined}），useEffect([index]) 触发 scrollIntoView({behavior:"smooth",block:"nearest",inline:"nearest"}) 最小幅度滚入；所有切换路径（slideTo/箭头/灯箱 onIndexChange）最终都走 setIndex，单一入口覆盖。tsc/eslint 通过，未 commit。

## 详情页 keywords + 资源编辑编辑器同步（2026-09-08）
- resources/[slug]/page.tsx generateMetadata 加 keywords: r.tags.map(t=>t.tag.name)（数组），详情页 meta 关键词改用资源标签（此前只有根布局全局 keywords）
- ResourceEditForm 的 description 由裸 textarea 换成 MdEditor（与发布向导一致）：description state + hidden input 模式，defaultValue=resource.description 预填；admin/content/[id]/edit 与 resources/[slug]/edit 共用组件同时受益
- tsc/eslint 通过，未 commit

## 账号隐私设置（2026-09-08）
用户可配置主页收藏/粉丝/关注列表是否对外展示：
- User 加 showFavorites(默认false)/showFollowers(默认true)/showFollowing(默认true)，延续原行为（收藏本来仅本人可见）；prisma db push 已同步（generate 的 dll rename 被锁报 EPERM 但 engine 未变、JS/TS 类型已生成，tmp 残留已清）
- queries.ts UserProfile/getProfile 带出三字段
- actions/settings.ts 加 updatePrivacyAction（checkbox "on" 语义，revalidate /u/xxx + /settings）
- 新建 components/auth/privacy-form.tsx（SquareCheckbox 非受控 + useActionState），settings 页「隐私设置」区块
- u/[username] TABS 的 selfOnly 改为 privacy key 映射，tabVisible = 本人恒可见 || 对方开关；URL 强进被挡回落作品 tab；统计数字保留（仅列表受控）
- tsc/eslint 通过，未 commit

## AI 配置与云盘配置迁入站点配置（2026-09-08）
- runtime-config 加 aiBaseUrl/aiApiKey/aiModel（env AI_PROVIDER_* 回退）；providerFromEnv 改 async providerFromConfig 读后台配置，ai.ts 两处适配（AiRun.model 记录也走 cfg，getRuntimeConfig 提前到事务前）
- RuntimeConfigManager 加「云盘附件（Microsoft Graph）」「网站管家 AI」两个 section（form state 加 8 字段；提交全量合并 {...config,...form} 天然兼容）
- DriveManager 拆掉 GraphCredsCard（凭据编辑收敛到站点配置，避免两处编辑同一字段），props 精简回 rows+credsConfigured；未配置提示改指向 /admin/runtime
- 教训：providerFromConfig() async 化后不能 await p().complete() 链式调用（TS2339），先 await 再取方法
- tsc/eslint 通过，未 commit

## 附件走云盘开关（2026-09-08）
站点配置存储卡新增「附件上传走云盘」：runtime-config 加 attachmentCloud 三态 z.enum(auto/on/off) 默认 auto；生效值 attachmentCloudEnabled = on→true / off→false / auto→driver==="chevereto"。RuntimeConfigManager 存储卡 checkbox 受控映射（checked = on || (auto && chevereto)，点击固化为 on/off，切驱动时 auto 状态自动反映）。两个附件上传路由判定点接入（cloud = graphEnabled && attachmentCloudEnabled ? activeDrive : null），关闭时走既有 NO_CLOUD 回退（客户端回退存储驱动直传）。tsc/eslint 通过，未 commit。

## 拆分运行配置 tab（2026-09-08）
RuntimeConfigManager 内部引入 SubTabs：登录/存储/邮件/云盘/AI 五个分组 tab（hidden 显隐，表单状态跨切换保留），共享保存按钮放 tab 外（单表单全量提交语义不变）。页面级「运行配置|SEO 配置」SubTabs 保留，两层结构。python 重排 section 时 head 切片多带旧 return 行（双 return），tsc 前手工修复——教训：python 大段重排后先 grep 结构再验证。tsc/eslint 通过，未 commit。
